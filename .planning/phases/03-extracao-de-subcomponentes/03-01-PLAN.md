---
phase: 03-extracao-de-subcomponentes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Simulados.tsx
  - src/components/SimuladoSetup.tsx
  - src/components/SimuladoHistorico.tsx
  - src/components/ui/index.ts
autonomous: true
requirements: [REFAC-02]
must_haves:
  truths:
    - "Tela de configuração do simulado aparece na etapa 'setup' com opções de quantidade e tempo"
    - "Histórico de simulados é exibido com lista e gráfico de tendência"
    - "Modal de prescrição tática abre ao clicar em 'Ver Prescrição' no histórico"
    - "Estado vazio do histórico é mostrado quando não há simulados realizados"
  artifacts:
    - path: "src/components/SimuladoSetup.tsx"
      provides: "Tela de configuração do simulado (cabeçalho, seletor qtd/tempo, card 'Como funciona')"
      min_lines: 150
    - path: "src/components/SimuladoHistorico.tsx"
      provides: "Histórico de simulados com lista, gráfico sparkline, modal de prescrição e estado vazio"
      min_lines: 200
    - path: "src/pages/Simulados.tsx"
      provides: "Página principal reduzida que orquestra os sub-componentes"
      max_lines: 600
    - path: "src/components/ui/index.ts"
      provides: "Barrel export atualizado com novos componentes"
      exports: ["SimuladoSetup", "SimuladoHistorico"]
  key_links:
    - from: "src/pages/Simulados.tsx"
      to: "src/components/SimuladoSetup.tsx"
      via: "import + JSX no bloco etapa === 'setup'"
      pattern: "SimuladoSetup"
    - from: "src/pages/Simulados.tsx"
      to: "src/components/SimuladoHistorico.tsx"
      via: "import + JSX no bloco etapa === 'setup' dentro da coluna direita"
      pattern: "SimuladoHistorico"
---

<objective>
Extrair `SimuladoSetup` e `SimuladoHistorico` de `Simulados.tsx` (~983 linhas → ~600 linhas).

**Purpose:** Isolar a tela de configuração e o histórico de simulados em componentes dedicados com props tipadas, seguindo o padrão Questoes.tsx. Cada sub-componente recebe dados via props, nunca importa hooks diretamente (D-02).

**Output:** `SimuladoSetup.tsx`, `SimuladoHistorico.tsx`, `Simulados.tsx` modificado, barrel export atualizado.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-extracao-de-subcomponentes/03-CONTEXT.md
@src/pages/Simulados.tsx
@src/components/QuestaoVisualizador.tsx

<interfaces>
From src/hooks/useSimulados.ts (hook que a página consome):
```typescript
export type SimuladoEtapa = 'setup' | 'active' | 'submitting' | 'results'

export interface SimuladoConfig {
  qtdQuestoes: number
  tempoMinutos: number
}

export function useSimulados() {
  // Retorna:
  loading: boolean
  error: string | null
  etapa: SimuladoEtapa
  questoesSelected: ResolucaoView[]
  respostasMarcadas: Record<number, string>
  questaoAtualIndex: number
  setQuestaoAtualIndex: (i: number | ((prev: number) => number)) => void
  tempoRestante: number
  tempoGasto: number
  loadingFeedback: boolean
  diagnosticoIA: string | null
  pontuacao: { taxa: number; acertos: number; total: number } | null
  historicoSimulados: Array<{
    id?: number; data: string; qtdQuestoes: number; acertos: number;
    total: number; taxa: number; tempoGasto: number; tempoMinutos: number;
    diagnosticoIA?: string | null
  }>
  handleIniciarSimulado: (qtd: number, tempoMin: number) => void
  handleMarcarResposta: (questaoId: number, letra: string) => void
  handleFinalizarSimulado: () => void
  handleResetSimulado: () => void
  handleLimparHistorico: () => void
}
```

From src/lib/cleanHtml.ts:
```typescript
export function cleanHtmlText(text: string | null | undefined): string
```

Padrão de componente extraído (D-01, D-02):
```typescript
// Props tipadas no mesmo arquivo
interface ComponenteProps {
  // dados via props, nunca hooks
  prop1: tipo
  onAction: () => void
}
// Export nomeado
export function Componente({ prop1, onAction }: ComponenteProps) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Extrair SimuladoSetup de Simulados.tsx</name>
  <files>
    src/components/SimuladoSetup.tsx
    src/pages/Simulados.tsx
    src/components/ui/index.ts
  </files>
  <action>
    **Criar `src/components/SimuladoSetup.tsx`:**

    1. Copiar DO BLOCO `etapa === 'setup'` de Simulados.tsx:
       - O `formatCountdown` helper (linhas 40-44) — mover para este arquivo como função privada (não exportada)
       - O cabeçalho com `BrainCircuit` + título + descrição (linhas 142-151)
       - O grid de 12 colunas que contém:
         - Card esquerdo (lg:col-span-5): "Ajustar Parâmetros da Prova" com:
           - Seletor de quantidade (botões 10/15/20 com `selectedQtd`/`setSelectedQtd`)
           - Seletor de tempo (botões 15/20/30 com `selectedTempo`/`setSelectedTempo`)
           - Botão "Gerar Simulado Personalizado"
         - Card "Como funciona" (fora do grid, abaixo do card esquerdo)
       - O mobile accordion toggle do histórico é parte do SimuladoHistorico — NÃO incluir aqui
       - A coluna direita (lg:col-span-7) com histórico e gráfico — NÃO incluir, é do SimuladoHistorico

    2. **Props do SimuladoSetup:**
    ```typescript
    interface SimuladoSetupProps {
      selectedQtd: number
      selectedTempo: number
      onSelectQtd: (qtd: number) => void
      onSelectTempo: (tempo: number) => void
      onIniciarSimulado: () => void
      historicoCount: number
      isHistoryExpandedMobile: boolean
      onToggleHistoryMobile: () => void
    }
    ```

    3. **No `Simulados.tsx`:**
       - REMOVER do bloco `etapa === 'setup'` o cabeçalho, o grid de config, o card "Como funciona", e o mobile toggle.
       - Importar `SimuladoSetup`
       - SUBSTITUIR a seção removida por:
       ```tsx
       <SimuladoSetup
         selectedQtd={selectedQtd}
         selectedTempo={selectedTempo}
         onSelectQtd={setSelectedQtd}
         onSelectTempo={setSelectedTempo}
         onIniciarSimulado={() => handleIniciarSimulado(selectedQtd, selectedTempo)}
         historicoCount={historicoSimulados.length}
         isHistoryExpandedMobile={isHistoryExpandedMobile}
         onToggleHistoryMobile={() => setIsHistoryExpandedMobile(!isHistoryExpandedMobile)}
       />
       ```
       OBS: manter o `onClick` do botão Gerar diretamente inline no `onIniciarSimulado` para manter o closure com os states.
       - REMOVER o helper `formatCountdown` (não usado mais no setup — só usado no active)

    4. **Atualizar barrel:** Adicionar `export { SimuladoSetup } from '../SimuladoSetup'` em `src/components/ui/index.ts`.

    **Restrições:**
    - D-02: Não importar hooks. Props only.
    - Preservar TODOS os estilos, cores, animações, gradientes exatamente como estão.
    - Não alterar comportamento — apenas mover JSX para o novo componente.
    - Manter a estrutura aninhada exata (divs, classes, etc).
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <done>SimuladoSetup.tsx criado com props tipadas. Simulados.tsx usa &lt;SimuladoSetup&gt; no lugar da seção de config. tsc zero erros.</done>
</task>

<task type="auto" tdd="false">
  <name>task 2: Extrair SimuladoHistorico de Simulados.tsx</name>
  <files>
    src/components/SimuladoHistorico.tsx
    src/pages/Simulados.tsx
    src/components/ui/index.ts
  </files>
  <action>
    **Criar `src/components/SimuladoHistorico.tsx`:**

    1. Copiar de Simulados.tsx TODO o conteúdo relacionado ao histórico dentro do bloco `etapa === 'setup'`:
       - A coluna direita inteira (dentro do `<div className={`lg:col-span-7 space-y-6 ${isHistoryExpandedMobile ? 'block animate-fade-in-up' : 'hidden lg:block'}`}>`)
       - Que contém: estado vazio (quando `historicoSimulados.length === 0`) OU
         - Lista de histórico com cabeçalho + botão limpar
         - Lista de itens com paginação (verTodosHistorico)
         - Botão "Ver todos" / "Ver menos"
         - Painel de sparkline (gráfico AreaChart)
       - O Modal de prescrição tática (`selectedSimuladoForModal`) — NÃO incluir no componente, o modal fica no mesmo nível do componente na página

    2. **Props do SimuladoHistorico:**
    ```typescript
    import type { SimuladoHistoricoItem } from '../types/database'
    
    // Ou definir inline:
    interface HistoricoItem {
      id?: number
      data: string
      qtdQuestoes: number
      acertos: number
      total: number
      taxa: number
      tempoGasto: number
      tempoMinutos: number
      diagnosticoIA?: string | null
    }

    interface SimuladoHistoricoProps {
      historico: HistoricoItem[]
      verTodos: boolean
      onToggleVerTodos: () => void
      onRefazer: (qtd: number, tempoMin: number) => void
      onVerPrescricao: (item: HistoricoItem) => void
      onLimparHistorico: () => void
    }
    ```

    3. **No `Simulados.tsx`:**
       - REMOVER a coluna direita inteira do setup (todo o `<div className={`lg:col-span-7...`}>...</div>`)
       - Importar `SimuladoHistorico`
       - SUBSTITUIR por:
       ```tsx
       <SimuladoHistorico
         historico={historicoSimulados}
         verTodos={verTodosHistorico}
         onToggleVerTodos={() => setVerTodosHistorico(!verTodosHistorico)}
         onRefazer={(qtd, tempoMin) => handleIniciarSimulado(qtd, tempoMin)}
         onVerPrescricao={(item) => setSelectedSimuladoForModal(item)}
         onLimparHistorico={handleLimparHistorico}
       />
       ```
       - O Modal de prescrição permanece em Simulados.tsx, no mesmo nível do SimuladoHistorico (dentro do fragmento `<>`) — manter `selectedSimuladoForModal` e seu JSX intactos.

    4. **Dentro de `SimuladoHistorico.tsx`:**
       - IMPORTAR `formatarTempo` de `'../hooks/useDashboard'` (já re-exportado)
       - Copiar os imports de recharts necessários: `AreaChart, Area, ResponsiveContainer, Tooltip`
       - Copiar os imports de lucide-react necessários: `History, Trash2, Award, Sparkles, ChevronDown, ChevronUp, ClipboardList`
       - Criar o gráfico com `chartData` computado internamente com useMemo (mesma lógica das linhas 129-136)
       - Manter o estado vazio inline (quando historico.length === 0)

    5. **Atualizar barrel:** Adicionar `export { SimuladoHistorico } from '../SimuladoHistorico'` em `src/components/ui/index.ts`.

    **Restrições:**
    - D-02: Props only — não importar hooks.
    - `useMemo` e `useState` são permitidos pois são hooks de React, não hooks de dados da aplicação.
    - O componente gerencia seus próprios estados de UI (verTodos, isHistoryExpandedMobile) via props que o pai controla.
    - Manter exatamente os mesmos estilos visuais.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit; if ($?) { npm test }</automated>
  </verify>
  <done>SimuladoHistorico.tsx criado. Simulados.tsx usa &lt;SimuladoHistorico&gt;. Modal de prescrição mantido na página. tsc zero erros, 38+ testes passando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| componente←props | Props são passadas pelo pai confiável (Simulados.tsx) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | SimuladoHistorico | accept | Componente apenas renderiza dados recebidos via props; sem entrada de usuário |
| T-03-02 | T | SimuladoSetup | accept | Botões chamam callbacks passados pelo pai; sem estado sensível |
</threat_model>

<verification>
1. `npx tsc -b --noEmit` — zero erros
2. `npx eslint . --max-warnings=200` — zero erros
3. `npm test` — 38+ testes passando
4. Revisar visual: tela de setup com qtd/tempo aparece, histórico lista simulados
</verification>

<success_criteria>
- Simulados.tsx reduzido de ~983 para ~600 linhas
- SimuladoSetup.tsx e SimuladoHistorico.tsx criados com props tipadas
- D-02 respeitado: componentes não importam hooks
- Barrel export atualizado
</success_criteria>

<output>
After completion, create `.planning/phases/03-extracao-de-subcomponentes/03-01-SUMMARY.md`
</output>
