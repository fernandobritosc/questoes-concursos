---
phase: 03-extracao-de-subcomponentes
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/pages/Simulados.tsx
  - src/components/SimuladoExamView.tsx
  - src/components/SimuladoResultados.tsx
  - src/components/ui/index.ts
autonomous: true
requirements: [REFAC-02]
must_haves:
  truths:
    - "Tela de prova ativa exibe barra superior com cronômetro, enunciado, alternativas e grade de navegação"
    - "Tela de submissão mostra loading com mensagem de avaliação IA"
    - "Tela de resultados exibe cabeçalho, cartões de desempenho, diagnóstico IA e revisão detalhada"
    - "Revisão detalhada tem accordions expansíveis com enunciado, alternativas e resolução"
  artifacts:
    - path: "src/components/SimuladoExamView.tsx"
      provides: "Tela de prova ativa com barra superior, questão, alternativas e grade de navegação"
      min_lines: 200
    - path: "src/components/SimuladoResultados.tsx"
      provides: "Tela de resultados com cabeçalho, cartões, diagnóstico IA e revisão detalhada"
      min_lines: 280
    - path: "src/components/ui/index.ts"
      provides: "Barrel export atualizado"
      exports: ["SimuladoExamView", "SimuladoResultados"]
    - path: "src/pages/Simulados.tsx"
      provides: "Página principal totalmente refatorada"
      max_lines: 350
  key_links:
    - from: "Simulados.tsx"
      to: "SimuladoExamView.tsx"
      via: "import + JSX no bloco etapa === 'active'"
    - from: "Simulados.tsx"
      to: "SimuladoResultados.tsx"
      via: "import + JSX no bloco etapa === 'results'"
---

<objective>
Extrair `SimuladoExamView` e `SimuladoResultados` de `Simulados.tsx` (~600 linhas pós-Plan01 → ~330 linhas).

**Purpose:** Completar a refatoração de Simulados.tsx extraindo a tela de prova ativa e a tela de resultados. A tela de resultados inclui os cartões de desempenho, diagnóstico IA e a revisão detalhada com accordions.

**Dependency:** Plan 01 (precisa do Simulados.tsx já reduzido) — executa após o Plan 01.

**Output:** `SimuladoExamView.tsx`, `SimuladoResultados.tsx`, `Simulados.tsx` modificado, barrel export atualizado.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-extracao-de-subcomponentes/03-CONTEXT.md
@.planning/phases/03-extracao-de-subcomponentes/03-01-SUMMARY.md
@src/pages/Simulados.tsx

<interfaces>
O Simulados.tsx após o Plan 01 terá esta estrutura de estados/hook:
```typescript
// Estados locais
const [selectedQtd, setSelectedQtd] = useState(10)
const [selectedTempo, setSelectedTempo] = useState(15)
const [selectedSimuladoForModal, setSelectedSimuladoForModal] = useState<any | null>(null)
const [verTodosHistorico, setVerTodosHistorico] = useState(false)
const [isHistoryExpandedMobile, setIsHistoryExpandedMobile] = useState(false)
const [activeReviewIndex, setActiveReviewIndex] = useState<number | null>(null)
const [explicacoesRevisao, setExplicacoesRevisao] = useState<Record<number, string>>({})
const [loadingExplicacao, setLoadingExplicacao] = useState<number | null>(null)

// Do hook
const {
  loading, error, etapa, questoesSelected, respostasMarcadas,
  questaoAtualIndex, setQuestaoAtualIndex, tempoRestante, tempoGasto,
  loadingFeedback, diagnosticoIA, pontuacao, historicoSimulados,
  handleIniciarSimulado, handleMarcarResposta, handleFinalizarSimulado,
  handleResetSimulado, handleLimparHistorico,
} = useSimulados()

// Helper
function formatCountdown(segundos: number): string { ... }
function handleGerarExplicacaoIA(q: ResolucaoView) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Extrair SimuladoExamView de Simulados.tsx</name>
  <files>
    src/components/SimuladoExamView.tsx
    src/pages/Simulados.tsx
    src/components/ui/index.ts
  </files>
  <action>
    **Criar `src/components/SimuladoExamView.tsx`:**

    1. Copiar TODO o bloco `etapa === 'active'` (antes da refatoração, identificado pelas linhas com comentário `// ─── 2. TELA DE PROVA ATIVA (EXAM VIEW)`):
       - Barra superior: info da questão, cronômetro, botão "Finalizar Simulado"
       - Layout 2 colunas:
         - Coluna esquerda (lg:col-span-3): metadados, enunciado, alternativas (com `handleMarcarResposta`), navegação inferior (anterior/próxima/finalizar)
         - Coluna direita (lg:col-span-1): grid de navegação numerado + dica
       - Incluir o helper `formatCountdown` como função privada não exportada

    2. **Props:**
    ```typescript
    interface SimuladoExamViewProps {
      questoes: ResolucaoView[]
      questaoAtualIndex: number
      onSetQuestaoAtualIndex: (index: number | ((prev: number) => number)) => void
      respostasMarcadas: Record<number, string>
      onMarcarResposta: (questaoId: number, letra: string) => void
      tempoRestante: number
      onFinalizarSimulado: () => void
    }
    ```

    3. **No Simulados.tsx:**
       - REMOVER TODO o bloco `if (etapa === 'active')` (todo o JSX, mantendo apenas o if)
       - Importar e substituir:
       ```tsx
       if (etapa === 'active') {
         return (
           <SimuladoExamView
             questoes={questoesSelected}
             questaoAtualIndex={questaoAtualIndex}
             onSetQuestaoAtualIndex={setQuestaoAtualIndex}
             respostasMarcadas={respostasMarcadas}
             onMarcarResposta={handleMarcarResposta}
             tempoRestante={tempoRestante}
             onFinalizarSimulado={() => {
               if (window.confirm('Tem certeza de que deseja finalizar o simulado agora?')) {
                 handleFinalizarSimulado()
               }
             }}
           />
         )
       }
       ```
       - REMOVER o helper `formatCountdown` de Simulados.tsx (já não será mais usado)
       - Manter o bloco `if (etapa === 'submitting')` inline (é pequeno)

    4. **Imports no componente:** `Timer, ChevronRight, ChevronLeft, ClipboardList` de lucide-react; `Button` de `./ui/Button`; `cleanHtmlText` de `../lib/cleanHtml`.

    5. **Atualizar barrel:** Adicionar `export { SimuladoExamView } from '../SimuladoExamView'`.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <done>SimuladoExamView.tsx criado. Tela de prova ativa substituída. tsc zero erros.</done>
</task>

<task type="auto">
  <name>task 2: Extrair SimuladoResultados de Simulados.tsx</name>
  <files>
    src/components/SimuladoResultados.tsx
    src/pages/Simulados.tsx
    src/components/ui/index.ts
  </files>
  <action>
    **Criar `src/components/SimuladoResultados.tsx`:**

    1. Copiar TODO o bloco `etapa === 'results'` (comentário `// ─── 4. TELA DE RESULTADOS`):
       - Cabeçalho de resultados + botão "Gerar Novo Simulado"
       - Grid de 3 cartões: Aproveitamento, Questões Corretas, Tempo de Resolução
       - Diagnóstico exclusivo do Mentor IA (com loadingFeedback, diagnosticoIA, MarkdownAI)
       - Revisão detalhada com accordions (activeReviewIndex, explicacoesRevisao, loadingExplicacao)
       - Cada accordion: enunciado, alternativas com indicadores de acerto/erro, resolução do professor / botão "Gerar Resolução com IA"

    2. **Props:**
    ```typescript
    interface SimuladoResultadosProps {
      pontuacao: { taxa: number; acertos: number; total: number }
      tempoGasto: number
      questoes: ResolucaoView[]
      respostasMarcadas: Record<number, string>
      diagnosticoIA: string | null
      loadingFeedback: boolean
      activeReviewIndex: number | null
      onToggleReview: (index: number | null) => void
      explicacoesRevisao: Record<number, string>
      loadingExplicacao: number | null
      onGerarExplicacao: (q: ResolucaoView) => void
      onReset: () => void
    }
    ```

    3. **No Simulados.tsx:**
       - REMOVER TODO o bloco `if (etapa === 'results' && pontuacao)` 
       - Importar e usar:
       ```tsx
       if (etapa === 'results' && pontuacao) {
         return (
           <SimuladoResultados
             pontuacao={pontuacao}
             tempoGasto={tempoGasto}
             questoes={questoesSelected}
             respostasMarcadas={respostasMarcadas}
             diagnosticoIA={diagnosticoIA}
             loadingFeedback={loadingFeedback}
             activeReviewIndex={activeReviewIndex}
             onToggleReview={(idx) => setActiveReviewIndex(idx)}
             explicacoesRevisao={explicacoesRevisao}
             loadingExplicacao={loadingExplicacao}
             onGerarExplicacao={handleGerarExplicacaoIA}
             onReset={handleResetSimulado}
           />
         )
       }
       ```

    4. **Imports no componente:** `Award, ArrowLeft, CheckCircle2, XCircle, Clock, ClipboardList, BrainCircuit, Sparkles, Loader2, BookOpen, ChevronUp, ChevronDown` de lucide-react; `MarkdownAI` de `./ui/MarkdownAI`; `Button` de `./ui/Button`; `cleanHtmlText` de `../lib/cleanHtml`; `formatarTempo` de `../hooks/useDashboard`.

    5. **Atualizar barrel:** Adicionar `export { SimuladoResultados } from '../SimuladoResultados'`.

    **Restrições:** 
    - Manter toda a lógica de cores dos accordions (verde para acerto, vermelho para erro)
    - Preservar botão "Gerar Resolução com IA" dentro do accordion
    - isApproved calculado localmente no componente (const isApproved = pontuacao.taxa >= 70)
  </action>
  <verify>
    <automated>npx tsc -b --noEmit; if ($?) { npm test }</automated>
  </verify>
  <done>SimuladoResultados.tsx criado. Tela de resultados substituída. Simulados.tsx reduzido para ~330 linhas. tsc zero erros, 38+ testes passando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| componente←props | Props passadas pelo pai confiável (Simulados.tsx) |
| exam←onMarcarResposta | Callback de resposta é passado ao pai |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | T | SimuladoExamView | accept | Renderiza dados; callbacks repassados ao hook |
| T-03-07 | T | SimuladoResultados | accept | Renderiza pontuação; sem estado mutável |
</threat_model>

<verification>
1. `npx tsc -b --noEmit` — zero erros
2. `npx eslint . --max-warnings=200` — zero erros
3. `npm test` — 38+ testes passando
4. Verificar: fluxo completo do simulado (active → submitting → results)
</verification>

<success_criteria>
- Simulados.tsx reduzido de ~983 para ~330 linhas (após 2 planos)
- 4 sub-componentes extraídos no total
- Fluxo completo do simulado preservado
- Barrel export atualizado
</success_criteria>

<output>
After completion, create `.planning/phases/03-extracao-de-subcomponentes/03-04-SUMMARY.md`
</output>
