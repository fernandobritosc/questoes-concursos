---
phase: 03-extracao-de-subcomponentes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Revisao.tsx
  - src/components/RevisaoStatsCards.tsx
  - src/components/RevisaoFilterBar.tsx
  - src/components/RevisaoFocusView.tsx
  - src/components/ui/index.ts
autonomous: true
requirements: [REFAC-02]
must_haves:
  truths:
    - "Visão de grade com 4 cartões de estatísticas (Erros Pendentes, Matérias, Tópicos, Método SM-2)"
    - "Barra de busca e ordenação funcional filtrando matérias/assuntos"
    - "Accordions de matérias com sub-tópicos exibem erros pendentes e progresso"
    - "Modo de foco exibe questão ampliada com alternativas, resolução do professor, e navegação"
  artifacts:
    - path: "src/components/RevisaoStatsCards.tsx"
      provides: "Grid de 4 cartões de estatísticas do caderno de erros"
      min_lines: 60
    - path: "src/components/RevisaoFilterBar.tsx"
      provides: "Barra de busca textual e seletor de ordenação"
      min_lines: 40
    - path: "src/components/RevisaoFocusView.tsx"
      provides: "Visão de foco com questão, alternativas, resolução, navegação e classificação SM-2"
      min_lines: 350
    - path: "src/pages/Revisao.tsx"
      provides: "Página principal reduzida"
      max_lines: 450
  key_links:
    - from: "Revisao.tsx"
      to: "RevisaoStatsCards.tsx"
      via: "import + JSX substituindo grid de 4 stat cards"
    - from: "Revisao.tsx"
      to: "RevisaoFilterBar.tsx"
      via: "import + JSX substituindo barra de busca/ordenação"
    - from: "Revisao.tsx"
      to: "RevisaoFocusView.tsx"
      via: "import + JSX substituindo modo de foco no Path A"
---

<objective>
Extrair `RevisaoStatsCards`, `RevisaoFilterBar` e `RevisaoFocusView` de `Revisao.tsx` (~845 linhas → ~400 linhas).

**Purpose:** Isolar as seções de estatísticas, filtros e a visão de foco em componentes dedicados. A visão de foco é a maior seção (~350 linhas) e contém a questão, alternativas, resolução do professor, explicação IA, navegação e classificação SM-2.

**Output:** `RevisaoStatsCards.tsx`, `RevisaoFilterBar.tsx`, `RevisaoFocusView.tsx`, `Revisao.tsx` modificado, barrel export atualizado.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-extracao-de-subcomponentes/03-CONTEXT.md
@src/pages/Revisao.tsx

<interfaces>
Tipos usados:
```typescript
import type { ResolucaoView } from '../types/database'

// Estrutura de grupo de erros
interface AssuntoGrupo {
  nome: string
  quantidade: number
  erros: ResolucaoView[]
}

interface MateriaGrupo {
  materia: string
  assuntos: AssuntoGrupo[]
  quantidadeTotal: number
  maisRecente: number
}

interface RevisaoStats {
  totalPendentes: number
  totalMaterias: number
  totalAssuntos: number
}
```

Do hook useRevisao:
```typescript
const {
  loading, erros, questaoAtual, setQuestaoAtualIndex, totalErros,
  alternativaSelecionada, setAlternativaSelecionada, revelado,
  salvandoResposta, explicacaoAtual, loadingExplicacao,
  handleConfirmarResposta, handleExplicacaoIA, handleClassificar, obterPrazosEstimados
} = useRevisao()
```

Do hook useSearchParams:
```typescript
const [searchParams, setSearchParams] = useSearchParams()
const materiaParam = searchParams.get('materia')
const assuntoParam = searchParams.get('assunto')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Extrair RevisaoStatsCards de Revisao.tsx</name>
  <files>
    src/components/RevisaoStatsCards.tsx
    src/pages/Revisao.tsx
    src/components/ui/index.ts
  </files>
  <action>
    **Criar `src/components/RevisaoStatsCards.tsx`:**

    1. Copiar de Revisao.tsx:
       - O grid de 4 cartões de estatísticas (linhas 664-710)
       - Cada cartão: ícone, título, valor, descrição

    2. **Props:**
    ```typescript
    interface RevisaoStatsCardsProps {
      totalPendentes: number
      totalMaterias: number
      totalAssuntos: number
    }
    ```

    3. **No Revisao.tsx (Path B - Visão de Grade):**
       - REMOVER o grid `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 shrink-0">` (linhas 664-710)
       - Importar e usar:
       ```tsx
       <RevisaoStatsCards
         totalPendentes={stats.totalPendentes}
         totalMaterias={stats.totalMaterias}
         totalAssuntos={stats.totalAssuntos}
       />
       ```

    4. **Imports necessários:** `BookOpen, Layers, Book, BookOpenCheck` de lucide-react.

    5. **Atualizar barrel:** Adicionar `export { RevisaoStatsCards } from '../RevisaoStatsCards'` em `ui/index.ts`.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <done>RevisaoStatsCards.tsx criado. Grid de stats substituído. tsc zero erros.</done>
</task>

<task type="auto">
  <name>task 2: Extrair RevisaoFilterBar de Revisao.tsx</name>
  <files>
    src/components/RevisaoFilterBar.tsx
    src/pages/Revisao.tsx
    src/components/ui/index.ts
  </files>
  <action>
    **Criar `src/components/RevisaoFilterBar.tsx`:**

    1. Copiar de Revisao.tsx a barra de filtro (linhas 713-741):
       - Input de busca com ícone Search
       - Select de ordenação com ícone Filter
       - Rótulo "Ordenar por:"

    2. **Props:**
    ```typescript
    type Ordenacao = 'mais_erros' | 'mais_recentes' | 'alfabetica'

    interface RevisaoFilterBarProps {
      busca: string
      onBuscaChange: (value: string) => void
      ordenacao: Ordenacao
      onOrdenacaoChange: (value: Ordenacao) => void
    }
    ```

    3. **No Revisao.tsx:**
       - REMOVER a div `<div className="flex flex-col sm:flex-row gap-3 items-center justify-between shrink-0">` (linhas 713-741)
       - Importar e usar:
       ```tsx
       <RevisaoFilterBar
         busca={busca}
         onBuscaChange={setBusca}
         ordenacao={ordenacao}
         onOrdenacaoChange={setOrdenacao}
       />
       ```

    4. **Imports necessários:** `Search, Filter, ChevronDown` de lucide-react.

    5. **Atualizar barrel:** Adicionar `export { RevisaoFilterBar } from '../RevisaoFilterBar'`.

    **Restrições:** Preservar exatamente os mesmos estilos e comportamento dos selects/inputs.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <done>RevisaoFilterBar.tsx criado. Barra de filtro substituída. tsc zero erros.</done>
</task>

<task type="auto">
  <name>task 3: Extrair RevisaoFocusView de Revisao.tsx</name>
  <files>
    src/components/RevisaoFocusView.tsx
    src/pages/Revisao.tsx
    src/components/ui/index.ts
  </files>
  <action>
    **Criar `src/components/RevisaoFocusView.tsx`:**

    1. Copiar de Revisao.tsx TODO o Path A (modo de foco), linhas 295-643:
       - Cabeçalho de navegação: botão "Voltar ao Caderno", título do foco, contador (activeInFilterIndex + 1 / errosFiltrados.length)
       - Question workspace (dentro do `questaoAtual ? (...)`):
         - Card principal: cabeçalho com metadados (QID, banca, ano), enunciado, alternativas
         - Card de Resolução do Professor (todo o bloco com expand, edit, save)
         - Caixa de Explicação IA (se existir e for diferente da resolucao)
         - Rodapé com navegação (anterior/próxima), botão "Me Explique (IA)", botão Responder, classificação SM-2
       - Estado "Questão não localizada" (linhas 637-641)

    2. **Props - organizar em grupos:**
    ```typescript
    interface RevisaoFocusViewProps {
      // Dados
      questaoAtual: ResolucaoView | null
      materiaParam: string | null
      assuntoParam: string | null
      activeInFilterIndex: number
      totalFiltrados: number
      
      // Estado de resposta
      alternativaSelecionada: string | null
      onSelectAlternativa: (letra: string) => void
      revelado: boolean
      
      // Resolução do professor
      resolucaoExpanded: boolean
      onToggleResolucao: () => void
      editingResolucao: boolean
      onStartEditResolucao: () => void
      onCancelEditResolucao: () => void
      resolucaoText: string
      onResolucaoTextChange: (text: string) => void
      onSaveResolucao: () => void
      savingResolucao: boolean
      
      // Explicação IA
      explicacaoAtual: string | null
      onExplicacaoIA: () => void
      loadingExplicacao: boolean
      
      // Ações
      onVoltar: () => void
      onAnterior: () => void
      onProxima: () => void
      onResponder: () => void
      podeAnterior: boolean
      podeProxima: boolean
      salvandoResposta: boolean
      
      // Classificação SM-2
      onClassificar: (quality: number) => void
      obterPrazos: (questaoId: number | undefined) => { dificil: number; bom: number; facil: number }
    }
    ```

    3. **No Revisao.tsx:**
       - REMOVER todo o Path A (linhas 268-643, do `if (materiaParam && assuntoParam)` até o fechamento do JSX antes do Path B)
       - Importar e usar no lugar:
       ```tsx
       if (materiaParam && assuntoParam) {
         if (errosFiltrados.length === 0) { /* manter tópico concluído inline */ }
         return (
           <RevisaoFocusView
             questaoAtual={questaoAtual}
             materiaParam={materiaParam}
             assuntoParam={assuntoParam}
             activeInFilterIndex={activeInFilterIndex}
             totalFiltrados={errosFiltrados.length}
             alternativaSelecionada={alternativaSelecionada}
             onSelectAlternativa={setAlternativaSelecionada}
             revelado={revelado}
             resolucaoExpanded={resolucaoExpanded}
             onToggleResolucao={() => setResolucaoExpanded(!resolucaoExpanded)}
             editingResolucao={editingResolucao}
             onStartEditResolucao={() => setEditingResolucao(true)}
             onCancelEditResolucao={() => {
               setResolucaoText(questaoAtual?.resolucao_professor || '')
               setEditingResolucao(false)
             }}
             resolucaoText={resolucaoText}
             onResolucaoTextChange={setResolucaoText}
             onSaveResolucao={handleSaveResolucao}
             savingResolucao={savingResolucao}
             explicacaoAtual={explicacaoAtual}
             onExplicacaoIA={handleExplicacaoIA}
             loadingExplicacao={loadingExplicacao}
             onVoltar={() => setSearchParams({})}
             onAnterior={() => {
               const prev = errosFiltrados[activeInFilterIndex - 1]
               const idx = erros.findIndex(e => e.questao_tec_id === prev.questao_tec_id)
               if (idx !== -1) { setQuestaoAtualIndex(idx); setAlternativaSelecionada(null) }
             }}
             onProxima={() => {
               const next = errosFiltrados[activeInFilterIndex + 1]
               const idx = erros.findIndex(e => e.questao_tec_id === next.questao_tec_id)
               if (idx !== -1) { setQuestaoAtualIndex(idx); setAlternativaSelecionada(null) }
             }}
             onResponder={() => handleConfirmarResposta(0)}
             podeAnterior={activeInFilterIndex > 0}
             podeProxima={activeInFilterIndex < errosFiltrados.length - 1}
             salvandoResposta={salvandoResposta}
             onClassificar={handleClassificar}
             obterPrazos={obterPrazosEstimados}
           />
         )
       }
       ```
       NOTA: Manter o bloco `if (errosFiltrados.length === 0)` com o "Tópico Concluído" inline em Revisao.tsx (é pequeno e específico da página).

    4. **Imports no componente:** `Book, BookOpen, Pencil, Check, Loader2, BrainCircuit, ExternalLink, ChevronLeft, ChevronRight, ArrowLeft, CheckCircle2, XCircle, ArrowRight` de lucide-react; `MarkdownAI` de `../components/ui/MarkdownAI`; `cleanHtmlText` de `../lib/cleanHtml`; `updateResolucaoProfessor` de `../services/supabase.service`; `useState, useEffect` de react; `Button` de `../components/ui/Button`.

    5. **Estados locais dentro do componente:**
       - `resolucaoExpanded`, `editingResolucao`, `resolucaoText`, `savingResolucao` — agora gerenciados via props do pai (since the page needs to keep state across navigation)

    **IMPORTANTE:** O RevisaoFocusView gerencia internamente os estados `resolucaoExpanded`, `editingResolucao`, `resolucaoText` e `savingResolucao` via props recebidas. O pai (Revisao.tsx) mantém os estados originais e os passa como props. Isso preserva o comportamento de sincronização com a navegação entre questões.

    6. **Atualizar barrel:** Adicionar `export { RevisaoFocusView } from '../RevisaoFocusView'`.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit; if ($?) { npm test }</automated>
  </verify>
  <done>RevisaoFocusView.tsx criado. Path A substituído. Revisao.tsx reduzido. tsc zero erros, 38+ testes passando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| componente←props | Props passadas pelo pai confiável (Revisao.tsx) |
| focus←onSaveResolucao | Salvamento de resolução via serviço externo (supabase) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | T | RevisaoFocusView | accept | Renderiza questão recebida via props; callbacks de save/classify repassados ao pai |
</threat_model>

<verification>
1. `npx tsc -b --noEmit` — zero erros
2. `npx eslint . --max-warnings=200` — zero erros
3. `npm test` — 38+ testes passando
4. Verificar: modo grade com stats + filtros, modo foco com questão e classificação SM-2
</verification>

<success_criteria>
- Revisao.tsx reduzido de ~845 para ~400 linhas
- 3 sub-componentes criados com props tipadas
- D-02 respeitado: componentes não importam hooks de dados
- Barrel export atualizado
</success_criteria>

<output>
After completion, create `.planning/phases/03-extracao-de-subcomponentes/03-03-SUMMARY.md`
</output>
