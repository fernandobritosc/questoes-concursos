---
phase: 03-extracao-de-subcomponentes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/MapaQuestoes.tsx
  - src/components/MapaStatsCards.tsx
  - src/components/MapaMateriaAccordion.tsx
  - src/components/MapaSqlSetupModal.tsx
  - src/components/ui/index.ts
autonomous: true
requirements: [REFAC-02]
must_haves:
  truths:
    - "Grid de 4 cartões de estatísticas gerais é exibido no topo do Mapa"
    - "Matérias são exibidas como accordions expansíveis com sub-tópicos"
    - "Cada sub-tópico mostra nome, status, barra de progresso e botões de PDF/Revisar"
    - "Modal SQL de ativação de nuvem abre ao clicar em 'Nuvem' quando indisponível"
  artifacts:
    - path: "src/components/MapaStatsCards.tsx"
      provides: "Grid de 4 cartões de estatísticas (Assuntos Mapeados, Resolvidas/Totais, Aproveitamento, Mais Estudado)"
      min_lines: 70
    - path: "src/components/MapaMateriaAccordion.tsx"
      provides: "Accordion de matéria com grid de sub-tópicos, legendas, botões de PDF/upload/Revisar"
      min_lines: 250
    - path: "src/components/MapaSqlSetupModal.tsx"
      provides: "Modal instrutivo para configurar sincronização em nuvem no Supabase"
      min_lines: 100
    - path: "src/pages/MapaQuestoes.tsx"
      provides: "Página principal reduzida"
      max_lines: 500
  key_links:
    - from: "MapaQuestoes.tsx"
      to: "MapaStatsCards.tsx"
      via: "import + JSX substituindo grid de 4 stat cards"
    - from: "MapaQuestoes.tsx"
      to: "MapaMateriaAccordion.tsx"
      via: "import + JSX substituindo listagem de matérias"
    - from: "MapaQuestoes.tsx"
      to: "MapaSqlSetupModal.tsx"
      via: "import + JSX substituindo modal SQL"
---

<objective>
Extrair `MapaStatsCards`, `MapaMateriaAccordion` e `MapaSqlSetupModal` de `MapaQuestoes.tsx` (~892 linhas → ~470 linhas).

**Purpose:** Isolar o grid de estatísticas, os accordions de matérias, e o modal SQL em componentes dedicados, seguindo o padrão estabelecido.

**Output:** `MapaStatsCards.tsx`, `MapaMateriaAccordion.tsx`, `MapaSqlSetupModal.tsx`, `MapaQuestoes.tsx` modificado, barrel export atualizado.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-extracao-de-subcomponentes/03-CONTEXT.md
@src/pages/MapaQuestoes.tsx

<interfaces>
Tipos usados no MapaQuestoes:
```typescript
interface SubTopicData {
  nome: string
  totalQuestoes: number
  totalTentativas: number
  acertos: number
  erros: number
  taxaAcerto: number
  status: 'excelente' | 'atencao' | 'critico' | 'nao_estudado'
}

interface SubjectData {
  nome: string
  totalQuestoes: number
  totalTentativas: number
  acertos: number
  erros: number
  taxaAcerto: number
  assuntos: SubTopicData[]
}

// Tipo do serviço de estudo
interface StudyMaterialMetadata {
  fileName: string
  originalSize: number
  compressedSize: number
  updatedAt: string
}
```

Funções de callback da página:
```typescript
// handleRevisar(materia, assunto) → navega para /app/questoes
// handleUploadPdf(materia, assunto, event) → upload de PDF
// handleOpenPdf(materia, assunto) → abre PDF
// handleDeletePdf(materia, assunto) → deleta PDF
// handleToggleStorageMode(mode) → alterna local/cloud
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Extrair MapaStatsCards de MapaQuestoes.tsx</name>
  <files>
    src/components/MapaStatsCards.tsx
    src/pages/MapaQuestoes.tsx
    src/components/ui/index.ts
  </files>
  <action>
    **Criar `src/components/MapaStatsCards.tsx`:**

    1. Copiar de MapaQuestoes.tsx o grid de estatísticas (linhas 478-544):
       - 4 cartões: Assuntos Mapeados, Resolvidas/Totais, Aproveitamento Geral, Mais Estudado
       - Cada cartão tem: ícone com gradiente, título, valor, descrição

    2. **Props:**
    ```typescript
    interface MapaStatsCardsProps {
      totalAssuntos: number
      totalQuestoes: number
      totalResolvidasUnicas: number
      totalTentativas: number
      totalAcertos: number
      aproveitamentoGeral: number
      topAssuntoEstudado: string
      subjectsCount: number
    }
    ```

    3. **No MapaQuestoes.tsx:**
       - REMOVER o grid de estatísticas (linhas 478-544)
       - Importar e usar:
       ```tsx
       <MapaStatsCards
         totalAssuntos={overallStats.totalAssuntos}
         totalQuestoes={overallStats.totalQuestoes}
         totalResolvidasUnicas={overallStats.totalResolvidasUnicas}
         totalTentativas={overallStats.totalTentativas}
         totalAcertos={overallStats.totalAcertos}
         aproveitamentoGeral={overallStats.aproveitamentoGeral}
         topAssuntoEstudado={overallStats.topAssuntoEstudado}
         subjectsCount={subjectsData.length}
       />
       ```

    4. **Imports necessários no componente:** `Layers, CheckCircle2, Percent, TrendingUp` de lucide-react.

    5. **Atualizar barrel:** Adicionar `export { MapaStatsCards } from '../MapaStatsCards'` em `ui/index.ts`.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <done>MapaStatsCards.tsx criado. Grid de 4 cartões substituído por &lt;MapaStatsCards&gt;. tsc zero erros.</done>
</task>

<task type="auto">
  <name>task 2: Extrair MapaMateriaAccordion de MapaQuestoes.tsx</name>
  <files>
    src/components/MapaMateriaAccordion.tsx
    src/pages/MapaQuestoes.tsx
    src/components/ui/index.ts
  </files>
  <action>
    **Criar `src/components/MapaMateriaAccordion.tsx`:**

    1. Copiar de MapaQuestoes.tsx TODO o bloco de listagem de matérias (linhas 547-791):
       - Loop sobre `subjectsData.map(subject => ...)`
       - Accordion header de matéria com nome, badges de desempenho, expand/collapse
       - Grid expansível de sub-tópicos
       - Legenda de níveis (primeiro card expandido)
       - Cada sub-tópico card: nome, status badge, barra de progresso, seção de material PDF (upload/abrir/excluir), botão "Revisar Assunto"
       - Estado vazio quando subjectsData.length === 0

    2. **Props:**
    ```typescript
    interface MapaMateriaAccordionProps {
      subjects: SubjectData[]
      expandedSubjects: Record<string, boolean>
      onToggleSubject: (name: string) => void
      // Callbacks PDF
      uploadingKey: string | null
      materialsMetadata: Record<string, StudyMaterialMetadata>
      onUploadPdf: (materia: string, assunto: string, e: React.ChangeEvent<HTMLInputElement>) => void
      onOpenPdf: (materia: string, assunto: string) => void
      onDeletePdf: (materia: string, assunto: string) => void
      // Callback navegação
      onRevisar: (materia: string, assunto: string) => void
    }
    ```

    3. **No MapaQuestoes.tsx:**
       - REMOVER a div inteira `<div className="space-y-4">` que contém a listagem de matérias (linhas 547-791)
       - Importar e usar:
       ```tsx
       <MapaMateriaAccordion
         subjects={subjectsData}
         expandedSubjects={expandedSubjects}
         onToggleSubject={toggleSubject}
         uploadingKey={uploadingKey}
         materialsMetadata={materialsMetadata}
         onUploadPdf={handleUploadPdf}
         onOpenPdf={handleOpenPdf}
         onDeletePdf={handleDeletePdf}
         onRevisar={handleRevisar}
       />
       ```

    4. **Imports necessários:** lucide-react icons (ChevronUp, ChevronDown, HelpCircle, Award, AlertTriangle, ShieldAlert, CheckCircle2, FileText, BookOpen, Trash2, Upload, Play, Layers), `formatSize` helper — copiar a função para dentro do arquivo do componente como função privada não exportada.

    5. **Atualizar barrel:** Adicionar `export { MapaMateriaAccordion } from '../MapaMateriaAccordion'`.

    **Restrições:**
    - Manter toda a lógica de cores/badges baseada em status (excelente/atencao/critico/nao_estudado)
    - Preservar tooltips, animações e estilos exatos
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <done>MapaMateriaAccordion.tsx criado. Listagem de matérias substituída. tsc zero erros.</done>
</task>

<task type="auto">
  <name>task 3: Extrair MapaSqlSetupModal de MapaQuestoes.tsx</name>
  <files>
    src/components/MapaSqlSetupModal.tsx
    src/pages/MapaQuestoes.tsx
    src/components/ui/index.ts
  </files>
  <action>
    **Criar `src/components/MapaSqlSetupModal.tsx`:**

    1. Copiar de MapaQuestoes.tsx:
       - A constante `sqlCode` (linhas 372-419) — mover para dentro do componente
       - A função `handleCopySql` (linhas 421-425) — mover para dentro do componente como handler local
       - O modal inteiro (linhas 794-887) com todo seu conteúdo JSX

    2. **Props:**
    ```typescript
    interface MapaSqlSetupModalProps {
      isOpen: boolean
      onClose: () => void
      onRetry: () => Promise<void>
    }
    ```

    3. **No MapaQuestoes.tsx:**
       - REMOVER o bloco `{showSqlModal && (...)}` (linhas 794-887)
       - REMOVER a constante `sqlCode`
       - REMOVER a função `handleCopySql`
       - Importar e usar:
       ```tsx
       <MapaSqlSetupModal
         isOpen={showSqlModal}
         onClose={() => setShowSqlModal(false)}
         onRetry={async () => {
           const isAvail = await checkCloudAvailability()
           if (isAvail) {
             setCloudAvailable(true)
             setStorageMode('cloud')
             localStorage.setItem('mapa_storage_mode', 'cloud')
             setShowSqlModal(false)
             alert('Sincronização na Nuvem ativada com sucesso!')
           } else {
             alert('A tabela ainda não foi detectada. Verifique se executou o SQL no editor do Supabase.')
           }
         }}
       />
       ```

    4. **Imports necessários no componente:** `Cloud, AlertCircle, X, Copy, Check` de lucide-react, `useState` de react.

    5. **Atualizar barrel:** Adicionar `export { MapaSqlSetupModal } from '../MapaSqlSetupModal'`.

    **Restrições:**
    - `sqlCode` constante e `handleCopySql` ficam dentro do componente
    - Estado `copiedSql` gerenciado localmente com `useState`
    - Preservar exatamente o mesmo layout, cores e comportamento
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <done>MapaSqlSetupModal.tsx criado. Modal SQL substituído. tsc zero erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| componente←props | Props passadas pelo pai confiável (MapaQuestoes.tsx) |
| modal←onRetry | Callback de retry faz chamada assíncrona para serviço externo |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | T | MapaMateriaAccordion | accept | Renderiza dados; callbacks de upload/file são repassados ao pai |
| T-03-04 | T | MapaSqlSetupModal | accept | Apenas exibe SQL pré-definido; sem input de usuário que execute SQL |
</threat_model>

<verification>
1. `npx tsc -b --noEmit` — zero erros
2. `npx eslint . --max-warnings=200` — zero erros
3. `npm test` — 38+ testes passando
4. Verificar visual: grid de 4 cards, accordions de matérias, modal SQL funcional
</verification>

<success_criteria>
- MapaQuestoes.tsx reduzido de ~892 para ~470 linhas
- 3 sub-componentes criados com props tipadas
- D-02 respeitado: componentes não importam hooks de dados
- Barrel export atualizado
</success_criteria>

<output>
After completion, create `.planning/phases/03-extracao-de-subcomponentes/03-02-SUMMARY.md`
</output>
