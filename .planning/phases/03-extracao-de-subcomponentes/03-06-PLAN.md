---
phase: 03-extracao-de-subcomponentes
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - src/pages/EditalVerticalizado.tsx
  - src/components/EditalSidebar.tsx
  - src/components/EditalAssuntoItem.tsx
  - src/components/EditalMateriaDetalhes.tsx
  - src/components/ui/index.ts
autonomous: true
requirements: [REFAC-02]
must_haves:
  truths:
    - "Sidebar esquerda lista matérias com busca e botão 'Nova Matéria'"
    - "Painel direito exibe detalhes da matéria selecionada: header, métricas, toolbar de busca/add"
    - "Cada assunto exibe nome, taxa de acerto, checkbox de conclusão e botões de reordenação"
    - "Filtros de status (Todos / Críticos / Não Iniciados) funcionam na listagem de assuntos"
  artifacts:
    - path: "src/components/EditalSidebar.tsx"
      provides: "Sidebar esquerda com lista de matérias, busca e formulário de nova matéria"
      min_lines: 120
    - path: "src/components/EditalAssuntoItem.tsx"
      provides: "Item de assunto individual com checkbox, metadados, reordenação e exclusão"
      min_lines: 120
    - path: "src/components/EditalMateriaDetalhes.tsx"
      provides: "Painel direito completo: header, métricas, toolbar, filtros e lista de assuntos"
      min_lines: 350
    - path: "src/pages/EditalVerticalizado.tsx"
      provides: "Página principal reduzida"
      max_lines: 350
  key_links:
    - from: "EditalVerticalizado.tsx"
      to: "EditalSidebar.tsx"
      via: "import + JSX substituindo coluna esquerda"
    - from: "EditalVerticalizado.tsx"
      to: "EditalMateriaDetalhes.tsx"
      via: "import + JSX substituindo coluna direita"
---

<objective>
Extrair `EditalSidebar`, `EditalAssuntoItem` e `EditalMateriaDetalhes` de `EditalVerticalizado.tsx` (~748 linhas → ~350 linhas).

**Purpose:** Isolar a sidebar de matérias, o item de assunto individual e o painel de detalhes em componentes dedicados. O EditalVerticalizado tem uma estrutura clara de duas colunas que se presta bem à extração.

**Output:** `EditalSidebar.tsx`, `EditalAssuntoItem.tsx`, `EditalMateriaDetalhes.tsx`, `EditalVerticalizado.tsx` modificado, barrel export atualizado.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-extracao-de-subcomponentes/03-CONTEXT.md
@src/pages/EditalVerticalizado.tsx

<interfaces>
Estados e lógica do EditalVerticalizado:
```typescript
// Estados de customização (localStorage)
const [customOrder, setCustomOrder] = useState<Record<string, string[]>>(() => { ... })
const [customMaterias, setCustomMaterias] = useState<string[]>(() => { ... })
const [studiedAssuntos, setStudiedAssuntos] = useState<Record<string, boolean>>(() => { ... })

// Estados de UI
const [selectedMateria, setSelectedMateria] = useState<string | null>(null)
const [newMateriaName, setNewMateriaName] = useState('')
const [newAssuntoName, setNewAssuntoName] = useState('')
const [materiaSearch, setMateriaSearch] = useState('')
const [assuntoSearch, setAssuntoSearch] = useState('')
const [showAddMateria, setShowAddMateria] = useState(false)
const [statusFiltro, setStatusFiltro] = useState<'todos' | 'criticos' | 'nao_iniciados'>('todos')

// Dados
const resolucoes: ResolucaoView[]  // fetchAllQuestoes
const uniqueMateriasList: string[]
const materiasFiltradas: string[]
const assuntosDaMateria: string[]
const assuntosFiltrados: string[]
const materiaMetrics: { totalQuestoes: number; taxaAcerto: number; resolvidosCount: number }

// Ações
function moveAssunto(index: number, direction: 'up' | 'down' | 'top' | 'bottom') { ... }
function toggleAssuntoStudied(assunto: string) { ... }
function handleAddMateria() { ... }
function handleAddAssunto() { ... }
function handleRemoveCustomMateria(materia: string, e: React.MouseEvent) { ... }
function handleRemoveAssunto(assunto: string, e: React.MouseEvent) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Extrair EditalSidebar de EditalVerticalizado.tsx</name>
  <files>
    src/components/EditalSidebar.tsx
    src/pages/EditalVerticalizado.tsx
    src/components/ui/index.ts
  </files>
  <action>
    **Criar `src/components/EditalSidebar.tsx`:**

    1. Copiar de EditalVerticalizado.tsx a coluna esquerda inteira (linhas 348-452):
       - A div `<div className={`w-full md:w-80 border-r ...`}>`
       - Caixa de busca de matérias com input
       - Botão/formulário "Nova Matéria" (showAddMateria toggle)
       - Lista com rolagem de matérias (materiasFiltradas.map)
       - Cada item: nome, badge Custom, contagem de questões, botão de remover (se custom), ChevronRight
       - Estado vazio "Nenhuma matéria encontrada"

    2. **Props:**
    ```typescript
    interface EditalSidebarProps {
      materias: string[]
      selectedMateria: string | null
      onSelectMateria: (materia: string) => void
      materiaSearch: string
      onMateriaSearchChange: (value: string) => void
      showAddMateria: boolean
      onToggleAddMateria: () => void
      newMateriaName: string
      onNewMateriaNameChange: (value: string) => void
      onAddMateria: () => void
      customMaterias: string[]
      onRemoveCustomMateria: (materia: string, e: React.MouseEvent) => void
      // Para contagem de questões por matéria
      getQuestaoCount: (materia: string) => number
    }
    ```

    3. **No EditalVerticalizado.tsx:**
       - REMOVER a coluna esquerda inteira (a div com `className="w-full md:w-80 border-r border-border ..."`)
       - Importar e usar:
       ```tsx
       <EditalSidebar
         materias={materiasFiltradas}
         selectedMateria={selectedMateria}
         onSelectMateria={setSelectedMateria}
         materiaSearch={materiaSearch}
         onMateriaSearchChange={setMateriaSearch}
         showAddMateria={showAddMateria}
         onToggleAddMateria={() => setShowAddMateria(!showAddMateria)}
         newMateriaName={newMateriaName}
         onNewMateriaNameChange={setNewMateriaName}
         onAddMateria={handleAddMateria}
         customMaterias={customMaterias}
         onRemoveCustomMateria={handleRemoveCustomMateria}
         getQuestaoCount={(materia) => resolucoes.filter(r => r.materia === materia).length}
       />
       ```

    4. **Imports necessários:** `Search, Plus, ChevronRight, Trash2` de lucide-react.

    5. **Atualizar barrel:** Adicionar `export { EditalSidebar } from '../EditalSidebar'`.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <done>EditalSidebar.tsx criado. Sidebar substituída. tsc zero erros.</done>
</task>

<task type="auto">
  <name>task 2: Extrair EditalAssuntoItem de EditalVerticalizado.tsx</name>
  <files>
    src/components/EditalAssuntoItem.tsx
    src/pages/EditalVerticalizado.tsx
    src/components/ui/index.ts
  </files>
  <action>
    **Criar `src/components/EditalAssuntoItem.tsx`:**

    1. Copiar de EditalVerticalizado.tsx o card de assunto individual (linhas 614-728):
       - Cada item renderizado dentro de `assuntosFiltrados.map((assunto) => { ... })`
       - Checkbox de conclusão (toggleAssuntoStudied)
       - Detalhes do assunto: número, nome, badge de taxa de acerto
       - Detalhes rápidos: contagem de questões, taxa
       - Controles de reordenação: top/up/down/bottom
       - Botão de excluir (com disabled se tem questões reais)

    2. **Props:**
    ```typescript
    interface EditalAssuntoItemProps {
      assunto: string
      index: number
      total: number
      isStudied: boolean
      onToggleStudied: () => void
      questaoCount: number
      taxaAcerto: number
      onMove: (direction: 'up' | 'down' | 'top' | 'bottom') => void
      onRemove: (e: React.MouseEvent) => void
      canRemove: boolean
    }
    ```

    3. **No EditalVerticalizado.tsx:**
       - REMOVER o JSX do assunto individual dentro do map (linhas 614-728), substituir por:
       ```tsx
       <EditalAssuntoItem
         key={assunto}
         assunto={assunto}
         index={originalIndex}
         total={assuntosDaMateria.length}
         isStudied={isStudied}
         onToggleStudied={() => toggleAssuntoStudied(assunto)}
         questaoCount={countQuestoes}
         taxaAcerto={taxaAcerto}
         onMove={(direction) => moveAssunto(originalIndex, direction)}
         onRemove={(e) => handleRemoveAssunto(assunto, e)}
         canRemove={!resolucoes.some(r => r.materia === selectedMateria && r.assunto === assunto)}
       />
       ```

    4. **Imports necessários:** `Check, ChevronsUp, ChevronUp, ChevronDown, ChevronsDown, Trash2, HelpCircle, CheckCircle2, XCircle` de lucide-react.

    5. **Atualizar barrel:** Adicionar `export { EditalAssuntoItem } from '../EditalAssuntoItem'`.

    **Restrições:** Manter a lógica de cores da borda esquerda (borderLeftStyle) baseada em isStudied, questaoCount e taxaAcerto.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <done>EditalAssuntoItem.tsx criado. Assuntos substituídos no map. tsc zero erros.</done>
</task>

<task type="auto">
  <name>task 3: Extrair EditalMateriaDetalhes de EditalVerticalizado.tsx</name>
  <files>
    src/components/EditalMateriaDetalhes.tsx
    src/pages/EditalVerticalizado.tsx
    src/components/ui/index.ts
  </files>
  <action>
    **Criar `src/components/EditalMateriaDetalhes.tsx`:**

    1. Copiar de EditalVerticalizado.tsx a coluna direita inteira (linhas 455-741):
       - A div `<div className={`flex-1 bg-muted/10 flex flex-col h-full overflow-hidden ${!selectedMateria ? 'hidden md:flex' : 'flex'}`}>`
       - O estado "Nenhuma matéria selecionada" (quando !selectedMateria)
       - Quando selectedMateria:
         - Header de detalhes: botão voltar mobile, título, métricas com SVG de progresso e taxa de acerto
         - Toolbar: busca de assuntos + input "Novo assunto" + botão Adicionar
         - Filtros rápidos: Todos / Críticos / Não Iniciados
         - Lista de assuntos com `EditalAssuntoItem` (recebido como prop children ou via render prop)
         - Estado vazio "Nenhum assunto catalogado"

    2. **Props:**
    ```typescript
    interface EditalMateriaDetalhesProps {
      selectedMateria: string
      onVoltar: () => void
      assuntosCount: number
      totalQuestoes: number
      taxaAcerto: number
      resolvidosCount: number
      assuntosTotal: number
      
      // Toolbar
      assuntoSearch: string
      onAssuntoSearchChange: (value: string) => void
      newAssuntoName: string
      onNewAssuntoNameChange: (value: string) => void
      onAddAssunto: () => void
      
      // Filtros
      statusFiltro: 'todos' | 'criticos' | 'nao_iniciados'
      onStatusFiltroChange: (filtro: 'todos' | 'criticos' | 'nao_iniciados') => void
      
      // Lista de assuntos (render props)
      assuntos: string[]
      renderAssunto: (assunto: string, index: number) => React.ReactNode
    }
    ```

    3. **No EditalVerticalizado.tsx:**
       - REMOVER a coluna direita inteira (a partir da div `flex-1 bg-muted/10 flex flex-col...`, linhas 455-741 até o fechamento final)
       - Importar e usar:
       ```tsx
       <EditalMateriaDetalhes
         selectedMateria={selectedMateria}
         onVoltar={() => setSelectedMateria(null)}
         assuntosCount={assuntosDaMateria.length}
         totalQuestoes={materiaMetrics.totalQuestoes}
         taxaAcerto={materiaMetrics.taxaAcerto}
         resolvidosCount={materiaMetrics.resolvidosCount}
         assuntosTotal={assuntosDaMateria.length}
         assuntoSearch={assuntoSearch}
         onAssuntoSearchChange={setAssuntoSearch}
         newAssuntoName={newAssuntoName}
         onNewAssuntoNameChange={setNewAssuntoName}
         onAddAssunto={handleAddAssunto}
         statusFiltro={statusFiltro}
         onStatusFiltroChange={setStatusFiltro}
         assuntos={assuntosFiltrados}
         renderAssunto={(assunto, originalIndex) => {
           // ...render EditalAssuntoItem aqui...
         }}
       />
       ```

       Na prática, como o `renderAssunto` precisa de muitos closures, pode ser mais prático manter o map dentro do EditalVerticalizado.tsx e passar a lista de JSX como children. Mas render props funciona bem para este caso.

       **Alternativa:** Em vez de render props, manter o map inline no EditalVerticalizado.tsx e renderizar o conteúdo dentro de uma prop `children` ou criar um slot para a lista.

       **Recomendação:** Usar a prop `children`:
       ```tsx
       <EditalMateriaDetalhes ...>
         {assuntosFiltrados.length === 0 ? (
           <EstadoVazio />
         ) : (
           assuntosFiltrados.map((assunto) => (
             <EditalAssuntoItem ... />
           ))
         )}
       </EditalMateriaDetalhes>
       ```
       Neste caso, o componente renderiza o header + toolbar + filtros e depois renderiza `{children}` no lugar da lista.

       DECISÃO: Usar `children` para a lista de assuntos. O componente recebe:
       ```typescript
       interface EditalMateriaDetalhesProps {
         selectedMateria: string | null
         onVoltar: () => void
         assuntosCount: number
         totalQuestoes: number
         taxaAcerto: number
         resolvidosCount: number
         assuntoSearch: string
         onAssuntoSearchChange: (value: string) => void
         newAssuntoName: string
         onNewAssuntoNameChange: (value: string) => void
         onAddAssunto: () => void
         statusFiltro: 'todos' | 'criticos' | 'nao_iniciados'
         onStatusFiltroChange: (filtro: 'todos' | 'criticos' | 'nao_iniciados') => void
         children: React.ReactNode   // Lista de assuntos
       }
       ```

    4. **Imports necessários:** `ChevronLeft, BookOpen, PieChart, Award, HelpCircle, Search, Plus, X` de lucide-react.

    5. **Atualizar barrel:** Adicionar `export { EditalMateriaDetalhes } from '../EditalMateriaDetalhes'`.

    **Restrições:** 
    - O SVG de círculo de progresso deve ser movido para o componente
    - Preservar exatamente as cores, animações e estilos
    - O estado "Nenhuma matéria selecionada" deve ser tratado internamente pelo componente
  </action>
  <verify>
    <automated>npx tsc -b --noEmit; if ($?) { npm test }</automated>
  </verify>
  <done>EditalMateriaDetalhes.tsx criado. Painel direito substituído. EditalVerticalizado.tsx reduzido para ~350 linhas. tsc zero erros, 38+ testes passando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| componente←props | Props passadas pelo pai confiável (EditalVerticalizado.tsx) |
| assunto←onRemove | Botão de excluir requer confirmação do usuário |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | T | EditalAssuntoItem | accept | Apenas renderiza dados recebidos via props; ações de remoção confirmadas via callback |
| T-03-12 | T | EditalSidebar | accept | Input de busca filtrado localmente; sem estado sensível |
| T-03-13 | T | EditalMateriaDetalhes | accept | Input de novo assunto é controlado; callback add valida duplicatas no pai |
</threat_model>

<verification>
1. `npx tsc -b --noEmit` — zero erros
2. `npx eslint . --max-warnings=200` — zero erros
3. `npm test` — 38+ testes passando
4. Verificar: sidebar com matérias, assuntos com reordenação, filtros de status
</verification>

<success_criteria>
- EditalVerticalizado.tsx reduzido de ~748 para ~350 linhas
- 3 sub-componentes criados com props tipadas
- D-02 respeitado: componentes não importam hooks de dados
- Barrel export atualizado
</success_criteria>

<output>
After completion, create `.planning/phases/03-extracao-de-subcomponentes/03-06-SUMMARY.md`
</output>
