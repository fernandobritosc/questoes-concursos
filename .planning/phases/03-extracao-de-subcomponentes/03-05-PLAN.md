---
phase: 03-extracao-de-subcomponentes
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - src/pages/Dashboard.tsx
  - src/components/DashboardMetricCard.tsx
  - src/components/DashboardResolucaoItem.tsx
  - src/components/DashboardStudyHeatmap.tsx
  - src/components/ui/index.ts
autonomous: true
requirements: [REFAC-02]
must_haves:
  truths:
    - "Cartões de métricas (Taxa de Acerto, Questões, Tempo, Erros) são exibidos com valores corretos"
    - "Heatmap de consistência de estudos (GitHub-style) é exibido com calendário de 365 dias"
    - "Lista de últimas resoluções é exibida com indicador de acerto/erro e link externo"
    - "Tooltip customizado do Recharts funciona nos gráficos"
  artifacts:
    - path: "src/components/DashboardMetricCard.tsx"
      provides: "Cartão de métrica com Sparkline e TrendIndicator"
      min_lines: 120
    - path: "src/components/DashboardResolucaoItem.tsx"
      provides: "Item de resolução com indicador de acerto/erro, tempo e link externo (inclui CustomTooltip)"
      min_lines: 70
    - path: "src/components/DashboardStudyHeatmap.tsx"
      provides: "Heatmap de consistência estilo GitHub com 365 dias de dados"
      min_lines: 200
    - path: "src/pages/Dashboard.tsx"
      provides: "Página Dashboard reduzida"
      max_lines: 450
  key_links:
    - from: "Dashboard.tsx"
      to: "DashboardMetricCard.tsx"
      via: "import + JSX substituindo uso de MetricCard inline"
    - from: "Dashboard.tsx"
      to: "DashboardResolucaoItem.tsx"
      via: "import + JSX substituindo ResolucaoItem inline"
    - from: "Dashboard.tsx"
      to: "DashboardStudyHeatmap.tsx"
      via: "import + JSX substituindo StudyHeatmap inline"
---

<objective>
Extrair `DashboardMetricCard`, `DashboardResolucaoItem` e `DashboardStudyHeatmap` de `Dashboard.tsx` (~808 linhas → ~400 linhas).

**Purpose:** Dashboard.tsx já possui componentes co-locados (Sparkline, TrendIndicator, MetricCard, ResolucaoItem, CustomTooltip, StudyHeatmap). Movê-los para arquivos individuais segue o padrão de 1 componente por arquivo e reduz significativamente a página principal.

**Output:** `DashboardMetricCard.tsx`, `DashboardResolucaoItem.tsx`, `DashboardStudyHeatmap.tsx`, `Dashboard.tsx` modificado, barrel export atualizado.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-extracao-de-subcomponentes/03-CONTEXT.md
@src/pages/Dashboard.tsx

<interfaces>
Tipos e hooks usados:
```typescript
import type { ResolucaoView } from '../types/database'
import { useDashboard, formatarTempo } from '../hooks/useDashboard'

// useDashboard retorna:
const { loading, stats, resolucoes } = useDashboard()

// stats tem a estrutura:
interface DashboardStats {
  saudacao: string
  streak: number
  dataFormatada: string
  totalQuestoes: number
  totalAcertos: number
  taxaAcerto: number
  tempoFormatado: string
  errosPendentes: number
  chartData: MateriaStat[]
  ultimasResolucoes: ResolucaoView[]
  evolucaoDiaria: DiaEvolucao[]
  stats24h: Stats24h
  trends: {
    taxa: Microtrend
    resolvidas: Microtrend
    tempo: Microtrend
    erros: Microtrend
  }
}

export interface DiaEvolucao {
  data: string
  resolvidas: number
  acertos: number
  taxa: number
}

export interface Microtrend {
  value: number
  isImprovement: boolean
  label: string
}
```

Componentes atualmente co-locados em Dashboard.tsx (a extrair):
- `Sparkline` (linhas 27-55) — SVG sparkline
- `TrendIndicator` (linhas 59-76) — Indicador WoW
- `MetricCard` (linhas 80-107) — Cartão de métrica
- `MateriaBar` (linhas 111-145) — Barra de progresso por matéria
- `ResolucaoItem` (linhas 149-211) — Item de resolução
- `CustomTooltip` (linhas 215-238) — Tooltip Recharts
- `StudyHeatmap` (linhas 246-451) — Heatmap de consistência
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Extrair DashboardMetricCard (Sparkline + TrendIndicator + MetricCard)</name>
  <files>
    src/components/DashboardMetricCard.tsx
    src/pages/Dashboard.tsx
    src/components/ui/index.ts
  </files>
  <action>
    **Criar `src/components/DashboardMetricCard.tsx`:**

    1. Copiar de Dashboard.tsx TRÊS funções em um único arquivo:
       - `Sparkline` (linhas 27-55) — como função privada NÃO exportada
       - `TrendIndicator` (linhas 59-76) — como função privada NÃO exportada
       - `MetricCard` (linhas 80-107) — como componente NÃO exportado (usado internamente) ou exportar apenas MetricCard
       
       Na verdade, exportar apenas `DashboardMetricCard` que combina os três. O componente final agrupa Sparkline + TrendIndicator + MetricCard numa única interface pública.

    2. **Interface pública do componente exportado:**
    ```typescript
    export interface DashboardMetricCardProps {
      title: string
      value: string | number
      subtitle: string
      icon: React.ReactNode
      gradientClass: string
      sparkColor: string
      stagger?: string
      sizeClass?: string
      trend: { value: number; isImprovement: boolean; label: string }
    }

    export function DashboardMetricCard({
      title, value, subtitle, icon, gradientClass, sparkColor, stagger = '', sizeClass = 'text-[40px]', trend,
    }: DashboardMetricCardProps) {
      // Mesmo JSX do MetricCard original
    }
    ```

    3. **No Dashboard.tsx:**
       - REMOVER as funções `Sparkline` (linhas 27-55), `TrendIndicator` (linhas 59-76), `MetricCard` (linhas 80-107)
       - SUBSTITUIR `import { formatarTempo } from '../hooks/useDashboard'` (manter se ainda usado para ResolucaoItem)
       - Importar `DashboardMetricCard` no lugar de MetricCard
       - SUBSTITUIR `<MetricCard ...>` por `<DashboardMetricCard ...>` em todas as 4 ocorrências (linhas 660-706)

    4. **Atualizar barrel:** Adicionar `export { DashboardMetricCard } from '../DashboardMetricCard'` em `ui/index.ts`.

    **Restrições:**
    - Sparkline continua gerando IDs únicos para gradientes SVG (usar `useId()` do React ou sufixo único)
    - Preservar todos os estilos exatos
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <done>DashboardMetricCard.tsx criado com Sparkline + TrendIndicator + MetricCard. Dashboard.tsx usa &lt;DashboardMetricCard&gt;. tsc zero erros.</done>
</task>

<task type="auto">
  <name>task 2: Extrair DashboardResolucaoItem (ResolucaoItem + CustomTooltip)</name>
  <files>
    src/components/DashboardResolucaoItem.tsx
    src/pages/Dashboard.tsx
    src/components/ui/index.ts
  </files>
  <action>
    **Criar `src/components/DashboardResolucaoItem.tsx`:**

    1. Copiar de Dashboard.tsx:
       - `CustomTooltip` (linhas 215-238) — como função privada não exportada (usada apenas internamente pelo Recharts)
       - `ResolucaoItem` (linhas 149-211) — exportar como `DashboardResolucaoItem`

    2. **Interface:**
    ```typescript
    interface DashboardResolucaoItemProps {
      res: ResolucaoView
      index: number
    }
    ```

    3. **Imports necessários:** `CheckCircle2, XCircle, ExternalLink` de lucide-react; `Link` de 'react-router-dom'; `formatarTempo` de `../hooks/useDashboard`; `ResolucaoView` de `../types/database`.

    4. **No Dashboard.tsx:**
       - REMOVER as funções `ResolucaoItem` (linhas 149-211) e `CustomTooltip` (linhas 215-238)
       - Importar e usar `DashboardResolucaoItem` no lugar de `ResolucaoItem`
       - Manter o `import { formatarTempo }` se ainda for necessário para `MateriaBar` — verificar. Se `MateriaBar` não usa, remover.

    5. **Atualizar barrel:** Adicionar `export { DashboardResolucaoItem } from '../DashboardResolucaoItem'`.

    **Nota:** `CustomTooltip` fica dentro de `DashboardResolucaoItem.tsx` como função privada porque é usada nos gráficos Recharts do Dashboard.tsx. Para evitar dependência circular, exportar CustomTooltip também como export nomeado para uso nos gráficos do Dashboard.

    **Opção melhor:** Exportar `CustomTooltip` separadamente também:
    ```typescript
    export { type CustomTooltipProps, CustomTooltip }
    ```
    E importar em Dashboard.tsx para usar nos charts.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <done>DashboardResolucaoItem.tsx criado com ResolucaoItem + CustomTooltip exportados. Dashboard.tsx atualizado. tsc zero erros.</done>
</task>

<task type="auto">
  <name>task 3: Extrair DashboardStudyHeatmap</name>
  <files>
    src/components/DashboardStudyHeatmap.tsx
    src/pages/Dashboard.tsx
    src/components/ui/index.ts
  </files>
  <action>
    **Criar `src/components/DashboardStudyHeatmap.tsx`:**

    1. Copiar de Dashboard.tsx TODO o componente `StudyHeatmap` (linhas 246-451):
       - State `isExpanded` gerenciado localmente
       - useMemo para `porDia`
       - useMemo para `diasGrid, mesesRotulos`
       - Função `getContributionLevel`
       - levelClasses (cores do tema violeta)
       - useMemo para `colunasSemanas`
       - JSX completo do heatmap

    2. **Interface:**
    ```typescript
    interface DashboardStudyHeatmapProps {
      resolucoes: Array<{ alternativa: string | null; data_resolucao: string }>
    }
    ```

    3. **Imports necessários:** `useState, useMemo` de react; `Activity, ChevronDown, ChevronUp` de lucide-react.

    4. **No Dashboard.tsx:**
       - REMOVER todo o componente `StudyHeatmap` (linhas 246-451) e seu import
       - REMOVER a interface `StudyHeatmapProps` (linhas 242-244)
       - Importar e usar:
       ```tsx
       <DashboardStudyHeatmap resolucoes={resolucoes} />
       ```
       (substitui `<StudyHeatmap resolucoes={resolucoes} />` na linha 710)

    5. **Atualizar barrel:** Adicionar `export { DashboardStudyHeatmap } from '../DashboardStudyHeatmap'`.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <done>DashboardStudyHeatmap.tsx criado. Heatmap substituído. tsc zero erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| componente←props | Props passadas pelo pai confiável (Dashboard.tsx) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | T | DashboardMetricCard | accept | Renderiza valores recebidos via props |
| T-03-09 | T | DashboardResolucaoItem | accept | Links externos abrem em nova aba (target=_blank + rel=noopener) |
| T-03-10 | T | DashboardStudyHeatmap | accept | Renderiza dados de data/contagem; sem entrada de usuário |
</threat_model>

<verification>
1. `npx tsc -b --noEmit` — zero erros
2. `npx eslint . --max-warnings=200` — zero erros
3. `npm test` — 38+ testes passando
4. Verificar: métricas, heatmap, lista de resoluções e tooltips funcionando
</verification>

<success_criteria>
- Dashboard.tsx reduzido de ~808 para ~400 linhas
- 3 sub-componentes criados com props tipadas
- D-02 respeitado: componentes não importam hooks de dados
- Barrel export atualizado
</success_criteria>

<output>
After completion, create `.planning/phases/03-extracao-de-subcomponentes/03-05-SUMMARY.md`
</output>
