---
phase: 02-extracao-de-hooks
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01-PLAN.md
  - 02-02-PLAN.md
files_modified:
  - src/hooks/useQuestoes.ts
autonomous: true
requirements:
  - REFAC-01
user_setup: []

must_haves:
  truths:
    - "useQuestoes.ts é reescrito como orquestrador (~100 linhas) que compõe useQuestoesFilter, useQuestoesCaderno e useQuestoesResolucao"
    - "useQuestoes() exporta EXATAMENTE a mesma interface de antes — todos os campos do return de useQuestoes.ts original estão presentes"
    - "Os 3 hooks especializados são importados e compostos; o orquestrador só gerencia estado que não pertence a nenhum hook específico (resolucoes, loading, paginação, import modal)"
    - "Questoes.tsx não precisa ser modificado — a interface de useQuestoes() é idêntica"
    - "Os effects de coordenação (timer, reset timer, load historico, sync resolucaoText, initial load, filter change) estão no orquestrador"
    - "handleGerarCaderno, handleConfirmarResposta, handleSaveResolucao, handleEditQuestao são wrappers no orquestrador que coordenam entre hooks"
    - "TypeScript compila limpo, ESLint zero erros, 38/38 testes passam"
  artifacts:
    - path: "src/hooks/useQuestoes.ts"
      provides: "Orquestrador que compõe os 3 hooks e re-exporta interface idêntica"
      exports: ["useQuestoes", "FilterTab", "ObjetivoFilter", "StatusFilter", "ImportStatus", "CARREIRAS_DISPONIVEIS", "ESCOLARIDADES_DISPONIVEIS", "FORMACOES_DISPONIVEIS", "REGIOES_DISPONIVEIS", "FAVORITAS_OPCOES", "ENUNCIADOS_OPCOES"]
  key_links:
    - from: "src/hooks/useQuestoes.ts"
      to: "src/hooks/useQuestoesFilter.ts"
      via: "import { useQuestoesFilter }"
      pattern: "import.*useQuestoesFilter"
    - from: "src/hooks/useQuestoes.ts"
      to: "src/hooks/useQuestoesCaderno.ts"
      via: "import { useQuestoesCaderno }"
      pattern: "import.*useQuestoesCaderno"
    - from: "src/hooks/useQuestoes.ts"
      to: "src/hooks/useQuestoesResolucao.ts"
      via: "import { useQuestoesResolucao }"
      pattern: "import.*useQuestoesResolucao"
    - from: "src/hooks/useQuestoes.ts"
      to: "src/pages/Questoes.tsx"
      via: "Mesma interface de retorno — nenhuma mudança em Questoes.tsx"
      pattern: "N/A — verificação por grep: destructuring em Questoes.tsx continua funcionando"

---

<objective>
Reescrever `useQuestoes.ts` como orquestrador que compõe os 3 hooks especializados e re-exporta a mesma interface.

**Purpose:** Substituir o hook monolítico de ~847 linhas por um orquestrador de ~80-120 linhas que delega responsabilidades a hooks especializados, mantendo compatibilidade total com `Questoes.tsx`.

**Output:** `src/hooks/useQuestoes.ts` (reescrito)
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-extracao-de-hooks/02-CONTEXT.md
@src/hooks/useQuestoes.ts
@src/hooks/useQuestoesFilter.ts
@src/hooks/useQuestoesCaderno.ts
@src/hooks/useQuestoesResolucao.ts
@src/pages/Questoes.tsx
@src/types/database.ts
@src/services/supabase.service.ts
@src/components/QuestaoVisualizador.tsx
@src/components/QuestaoResolucaoProfessor.tsx
</context>

<interfaces>
**Interface que o orquestrador deve reproduzir exatamente** (todos os campos abaixo devem estar no return):

```typescript
// Main data
resolucoes, setResolucoes,
loading, loadingError,

// Pagination
page, totalPages, totalCount, pageLoading, pageLoadingError,
handleNavigatePage, PAGE_SIZE,

// Caderno state
cadernoQuestoes, setCadernoQuestoes,
isCadernoActive, setIsCadernoActive,
currentQuestaoIndex, setCurrentQuestaoIndex,
alternativaSelecionada, setAlternativaSelecionada,
revelado, setRevelado,
copiedId,
tempoSegundos,
salvandoResposta,
historicoQuestaoAtiva, loadingHistoricoAtivo,

// Resolução state
editingResolucao, setEditingResolucao,
resolucaoText, setResolucaoText,
resolucaoExpanded, setResolucaoExpanded,
savingResolucao,
explicacoes, loadingExplicacao,

// Filter state
filtros, setFiltros,
questoesExibidas,
objetivo, setObjetivo,
activeTab, setActiveTab,
searchTerm, setSearchTerm,
showSearchBox, setShowSearchBox,
selectedMaterias,
selectedAssuntos, setSelectedAssuntos,
selectedBancas,
selectedAnos,
selectedOrgaos,
selectedConcursos,
selectedCarreiras,
selectedEscolaridades,
selectedFormacoes,
selectedRegioes,
selectedFavoritas,
selectedEnunciados,
selectedStatus, setSelectedStatus,
isFilterExpanded, setIsFilterExpanded,
visibleQuestionsCount, setVisibleQuestionsCount,
expandedMateriaFolder, setExpandedMateriaFolder,
cadernoNome, setCadernoNome,
pastaDestino, setPastaDestino,
gerarEmSerie, setGerarEmSerie,

// Import state
isImportModalOpen, setIsImportModalOpen,

// Derived
materiasUnicas, materiasComAssuntos,
bancasUnicas, anosUnicos,
orgaosUnicos, concursosUnicos,
filteredQuestions, filteredCount,
totalFiltrosAtivos,

// Actions
handleToggleMateria, handleToggleAssunto,
handleToggleBanca, handleToggleAno, handleToggleOrgao, handleToggleConcurso,
handleToggleCarreira, handleToggleEscolaridade, handleToggleFormacao,
handleToggleRegiao, handleToggleFavorita, handleToggleEnunciado,
handleResetFilters,
getFilteredQuestions,
handleGerarCaderno,
handleCopy,
handleExplicacaoIA,
handleSaveResolucao,
handleConfirmarResposta,
loadHistoricoDaQuestao,
handleEditQuestao,
```

**OBSERVAÇÃO CRÍTICA:** A interface de Questoes.tsx destructuring (linhas 27–68) usa APENAS um subconjunto destes campos. Mas outros sub-componentes podem usar os campos restantes indiretamente via props. Todos os campos originais devem ser preservados.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever useQuestoes.ts como orquestrador</name>
  <files>
    src/hooks/useQuestoes.ts
  </files>

  <read_first>
    @src/hooks/useQuestoes.ts (TOTALIDADE — precisa ser reescrito)
    @src/hooks/useQuestoesFilter.ts
    @src/hooks/useQuestoesCaderno.ts
    @src/hooks/useQuestoesResolucao.ts
    @src/pages/Questoes.tsx (linhas 27–68: destructuring para saber o que a página efetivamente usa)
    @src/types/database.ts
  </read_first>

  <action>
    **REESCREVER** `src/hooks/useQuestoes.ts` por completo — o arquivo vira um orquestrador que:

    1. **Importa** os 3 hooks especializados:
    ```typescript
    import { useQuestoesFilter } from './useQuestoesFilter'
    import { useQuestoesCaderno } from './useQuestoesCaderno'
    import { useQuestoesResolucao } from './useQuestoesResolucao'
    ```

    2. **Re-exporta** tipos e constantes dos hooks especializados (para compatibilidade com imports existentes):
    ```typescript
    export type { FilterTab, ObjetivoFilter, StatusFilter, ImportStatus } from './useQuestoesFilter'
    export { CARREIRAS_DISPONIVEIS, ESCOLARIDADES_DISPONIVEIS, FORMACOES_DISPONIVEIS, REGIOES_DISPONIVEIS, FAVORITAS_OPCOES, ENUNCIADOS_OPCOES } from './useQuestoesFilter'
    ```

    3. **Implementa** `useQuestoes()` com a seguinte estrutura:

    **a) Main Data (permanece no orquestrador):**
    - `resolucoes: ResolucaoView[]` + `setResolucoes`
    - `loading: boolean` — inicial `true`
    - `loadingError: string | null` — inicial `null`

    **b) Pagination State (permanece no orquestrador):**
    - `page: number` (inicial 1), `totalPages: number` (inicial 0), `totalCount: number` (inicial 0)
    - `pageLoading: boolean` (inicial false), `pageLoadingError: string | null` (inicial null)
    - `filterOptions: { materias, bancas, anos, orgaos, concursos } | null` (inicial null)
    - `PAGE_SIZE = 200`
    - `abortControllerRef = useRef<AbortController | null>(null)`
    - `loadPage(targetPage, replace?)` — useCallback que chama `filter.buildServerFilters()` e `fetchPaginatedQuestoes()`
    - `handleNavigatePage(targetPage)` — useCallback

    **c) Import Modal State:**
    - `isImportModalOpen: boolean` + `setIsImportModalOpen`

    **d) Compor os 3 hooks:**
    ```typescript
    const filter = useQuestoesFilter(resolucoes)
    const caderno = useQuestoesCaderno({
      getFilteredQuestions: filter.getFilteredQuestions,
      resolucoes,
      setResolucoes,
    })
    const resolucao = useQuestoesResolucao({
      onQuestoesUpdated: useCallback((targetId: number, updates: Partial<ResolucaoView>) => {
        caderno.setCadernoQuestoes(prev => prev.map(q =>
          (q.questao_id || q.id) === targetId ? { ...q, ...updates } : q
        ))
        setResolucoes(prev => prev.map(r =>
          (r.questao_id || r.id) === targetId ? { ...r, ...updates } : r
        ))
      }, [caderno.setCadernoQuestoes, setResolucoes]),
    })
    ```

    **e) questoesExibidas (cross-cutting — combina cadernoQuestoes + filtros):**
    ```typescript
    const questoesExibidas = useMemo(() => {
      if (!filter.filtros) return caderno.cadernoQuestoes
      return caderno.cadernoQuestoes.filter(q => {
        for (const [key, val] of Object.entries(filter.filtros)) {
          if (String((q as unknown as Record<string, unknown>)[key] || `Sem ${key}`) !== val) return false
        }
        return true
      })
    }, [caderno.cadernoQuestoes, filter.filtros])
    ```

    **f) Effects de coordenação (permanecem no orquestrador):**

    **Sync resolucaoText na navegação** (mesma lógica do efeito atual em useQuestoes.ts linhas 215–225):
    ```typescript
    useEffect(() => {
      if (questoesExibidas.length > 0 && questoesExibidas[caderno.currentQuestaoIndex]) {
        const q = questoesExibidas[caderno.currentQuestaoIndex]
        resolucao.setResolucaoText(q.resolucao_professor || '')
        resolucao.setEditingResolucao(false)
        caderno.setAlternativaSelecionada(null)
        caderno.setRevelado(false)
      }
    }, [caderno.currentQuestaoIndex, questoesExibidas])
    ```

    **Timer increment** (extraído de useQuestoes.ts linhas 336–342):
    ```typescript
    useEffect(() => {
      if (caderno.revelado || questoesExibidas.length === 0) return
      const timer = setInterval(() => {
        caderno.setTempoSegundos(prev => prev + 1)
      }, 1000)
      return () => clearInterval(timer)
    }, [caderno.revelado, caderno.currentQuestaoIndex, questoesExibidas])
    ```

    **Timer reset** (extraído de useQuestoes.ts linhas 345–348):
    ```typescript
    useEffect(() => {
      caderno.setTempoSegundos(0)
    }, [caderno.currentQuestaoIndex])
    ```

    **Load historico na navegação** (extraído de useQuestoes.ts linhas 363–377):
    ```typescript
    useEffect(() => {
      if (questoesExibidas.length > 0 && questoesExibidas[caderno.currentQuestaoIndex]) {
        const q = questoesExibidas[caderno.currentQuestaoIndex]
        const targetId = q.questao_id || q.id
        if (targetId) {
          caderno.loadHistoricoDaQuestao(targetId)
        } else {
          caderno.setHistoricoQuestaoAtiva([])
        }
      } else {
        caderno.setHistoricoQuestaoAtiva([])
      }
    }, [caderno.currentQuestaoIndex, questoesExibidas])
    ```

    **Initial load** (extraído de useQuestoes.ts linhas 228–285):
    - Mantém a lógica exata com `console.log`, `Promise.race` com timeout, `AbortController`
    - Chama `buildServerFilters()` do filter hook para construir os filtros
    - Chama `fetchPaginatedQuestoes(1, PAGE_SIZE, serverFilters)` e `fetchFilterOptions()`
    - Atualiza `resolucoes`, caderno (via `setCadernoQuestoes`), paginação, filterOptions
    - Mantém variável `cancelled` e cleanup
    - **Importante:** Como `caderno` já foi definido acima, pode chamar `caderno.setCadernoQuestoes(result.data)` diretamente

    **Filter change** (extraído de useQuestoes.ts linhas 288–331):
    - Mantém a lógica exata de abort controller, load page 1 com filtros, reset navigation
    - Dependências: `filter.selectedMaterias`, `filter.selectedBancas`, `filter.selectedAnos`, `filter.selectedOrgaos`, `filter.selectedConcursos`
    - **Importante:** Como o initial load effect depende destes mesmos filtros, precisa do guard `if (loading) return` para não duplicar a carga inicial

    **g) Wrapped actions (coordenam entre hooks):**

    **handleGerarCaderno:**
    ```typescript
    const handleGerarCaderno = useCallback(() => {
      const questoesFiltradas = filter.getFilteredQuestions()
      if (questoesFiltradas.length === 0) return
      caderno.setCadernoQuestoes(questoesFiltradas)
      caderno.setCurrentQuestaoIndex(0)
      caderno.setAlternativaSelecionada(null)
      caderno.setRevelado(false)
      caderno.setIsCadernoActive(true)
      trackEvent('gerar_caderno', {
        quantidade: questoesFiltradas.length,
        total_filtros: filter.totalFiltrosAtivos,
      })
    }, [filter, caderno.setCadernoQuestoes, caderno.setCurrentQuestaoIndex,
        caderno.setAlternativaSelecionada, caderno.setRevelado, caderno.setIsCadernoActive])
    ```

    **handleConfirmarResposta (wrapper):**
    ```typescript
    const handleConfirmarResposta = useCallback(async () => {
      const questao = questoesExibidas[caderno.currentQuestaoIndex]
      if (!questao) return
      await caderno.handleConfirmarResposta(questao)
    }, [questoesExibidas, caderno.currentQuestaoIndex, caderno.handleConfirmarResposta])
    ```

    **handleEditQuestao (wrapper):**
    ```typescript
    const handleEditQuestao = useCallback(async (updatedFields: Partial<ResolucaoView>): Promise<boolean> => {
      const questao = questoesExibidas[caderno.currentQuestaoIndex]
      if (!questao) return false
      return caderno.handleEditQuestao(questao, updatedFields)
    }, [questoesExibidas, caderno.currentQuestaoIndex, caderno.handleEditQuestao])
    ```

    **handleSaveResolucao (wrapper):**
    ```typescript
    const handleSaveResolucao = useCallback(async () => {
      const questao = questoesExibidas[caderno.currentQuestaoIndex]
      if (!questao) return
      const targetId = questao.questao_id || questao.id
      if (!targetId) return
      const success = await resolucao.handleSaveResolucao(targetId, resolucao.resolucaoText)
      if (success) {
        const text = resolucao.resolucaoText
        caderno.setCadernoQuestoes(prev => prev.map(q =>
          (q.questao_id === targetId || q.id === targetId) ? { ...q, resolucao_professor: text } : q
        ))
        setResolucoes(prev => prev.map(r =>
          (r.questao_id === targetId || r.id === targetId) ? { ...r, resolucao_professor: text } : r
        ))
      }
    }, [questoesExibidas, caderno.currentQuestaoIndex, resolucao.handleSaveResolucao,
        resolucao.resolucaoText, caderno.setCadernoQuestoes, setResolucoes])
    ```

    **h) Construir o return object:**
    O return deve ser um único objeto contendo TODOS os campos listados na seção `<interfaces>` acima.

    A ordem não importa, mas a presença de cada campo SIM. A técnica mais segura é construir o return como uma combinação de spreads e campos explícitos:

    ```typescript
    return {
      // Main data
      resolucoes, setResolucoes,
      loading, loadingError,

      // Pagination
      page, totalPages, totalCount, pageLoading, pageLoadingError,
      handleNavigatePage, PAGE_SIZE,

      // From caderno hook (spread — contém cadernoQuestoes, isCadernoActive, currentQuestaoIndex, etc.)
      ...caderno,

      // From resolucao hook (spread — contém editingResolucao, resolucaoText, etc.)
      ...resolucao,

      // From filter hook (spread — contém objetivo, activeTab, searchTerm, selected*, etc.)
      ...filter,

      // Import modal
      isImportModalOpen, setIsImportModalOpen,

      // Cross-cutting override (deve vir DEPOIS dos spreads para sobrescrever)
      questoesExibidas,

      // Wrapped actions (devem vir DEPOIS dos spreads para sobrescrever)
      handleGerarCaderno,
      handleConfirmarResposta,
      handleEditQuestao,
      handleSaveResolucao,
    }
    ```

    **⚠️ VERIFICAÇÃO CRÍTICA:** Todos os campos devem ser testados após a reescrita. Os spreads de `caderno`, `resolucao` e `filter` NÃO podem conter nomes duplicados entre si (conforme verificado durante o planejamento — não há overlap). O campo `questoesExibidas` que vem do `filter` (se existir) deve ser sobrescrito pelo do orquestrador.

    **i) Verificar re-exports:**
    - `FilterTab`, `ObjetivoFilter`, `StatusFilter`, `ImportStatus` devem ser re-exportados de `useQuestoesFilter`
    - `CARREIRAS_DISPONIVEIS`, `ESCOLARIDADES_DISPONIVEIS`, `FORMACOES_DISPONIVEIS`, `REGIOES_DISPONIVEIS`, `FAVORITAS_OPCOES`, `ENUNCIADOS_OPCOES` devem ser re-exportados de `useQuestoesFilter`
    - O alias `type Resolucao = ResolucaoView` deve ser mantido para compatibilidade

    **IMPORTANTE — o que NÃO deve mudar:**
    - A assinatura de `useQuestoes()` continua idêntica (zero parâmetros, mesmo return)
    - `Questoes.tsx` não deve ser tocado
    - Nenhum import existente para `./useQuestoes` quebra

    **IMPORTANTE — cuidado com a ordem de declaração:**
    - `resolucoes` é declarado PRIMEIRO (antes dos hooks)
    - `filter = useQuestoesFilter(resolucoes)` é chamado com `resolucoes`
    - `caderno = useQuestoesCaderno({...})` usa `filter.getFilteredQuestions` e `resolucoes`/`setResolucoes`
    - `resolucao = useQuestoesResolucao({...})` usa callback que referencia `caderno.setCadernoQuestoes`
    - `questoesExibidas` usa `caderno.cadernoQuestoes` e `filter.filtros`
    - Todos os effects e wrappers vêm DEPOIS de `questoesExibidas`
    - O return vem por último
  </action>

  <verify>
    <automated>$checklist = @(
      @{name="TypeScript"; cmd="npx tsc -b --noEmit"},
      @{name="ESLint"; cmd="npx eslint . --max-warnings=200"},
      @{name="Tests"; cmd="npx vitest run"}
    ); $failed = $false; foreach ($item in $checklist) { Write-Host "=== $($item.name) ==="; $result = Invoke-Expression $item.cmd; if ($LASTEXITCODE -ne 0) { Write-Host "FAILED: $($item.name)"; $failed = $true } }; if (-not $failed) { Write-Host "ALL CHECKS PASSED" }</automated>
  </verify>

  <done>
    - `src/hooks/useQuestoes.ts` reescrito — ~80-120 linhas, compõe 3 hooks
    - `npx tsc -b --noEmit` zero erros
    - `npx eslint . --max-warnings=200` zero erros
    - `npx vitest run` — 38/38 testes passam
    - `Questoes.tsx` não foi modificado
    - A interface do return é idêntica à original (pode ser verificado comparando as chaves do return com grep)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verificar compatibilidade total da interface</name>
  <files>
    src/hooks/useQuestoes.ts
  </files>

  <read_first>
    @src/hooks/useQuestoes.ts (após a reescrita — verificar o return)
    @src/pages/Questoes.tsx (linhas 27–68: destructuring para confirmar compatibilidade)
  </read_first>

  <action>
    **Verificar que a interface do orquestrador é idêntica à original.**

    1. Extrair os nomes dos campos retornados pelo novo `useQuestoes()`:
       ```bash
       rg "^\s+\w+," src/hooks/useQuestoes.ts | Select-String -Pattern "^\s+\w+," | ForEach-Object { $_ -replace '^\s+', '' -replace ',$', '' }
       ```

    2. Comparar com os campos retornados pelo hook original (guardar uma referência ANTES de modificar o arquivo):
       ```bash
       # Extrair campos do return original (salvar antes de modificar)
       type src/hooks/useQuestoes.ts | Select-String -Pattern "^\s+\w+," > $env:TEMP\return_fields_original.txt
       ```

    3. Verificar se `Questoes.tsx` precisaria de mudanças:
       ```bash
       npx tsc -b --noEmit 2>&1
       ```
       Se TypeScript compila sem erros, a interface é compatível.

    4. **Verificação adicional com grep** — confirmar que todos os campos originais ainda existem no return.

    **Lista completa de campos para verificar** (41 campos que a interface original deve conter):
    ```
    resolucoes, setResolucoes, loading, loadingError,
    page, totalPages, totalCount, pageLoading, pageLoadingError, handleNavigatePage, PAGE_SIZE,
    cadernoQuestoes, setCadernoQuestoes, isCadernoActive, setIsCadernoActive,
    currentQuestaoIndex, setCurrentQuestaoIndex,
    alternativaSelecionada, setAlternativaSelecionada,
    revelado, setRevelado,
    explicacoes, loadingExplicacao,
    copiedId,
    editingResolucao, setEditingResolucao,
    resolucaoText, setResolucaoText,
    resolucaoExpanded, setResolucaoExpanded,
    savingResolucao,
    tempoSegundos, salvandoResposta,
    historicoQuestaoAtiva, loadingHistoricoAtivo,
    handleConfirmarResposta,
    setFiltros,
    questoesExibidas,
    handleEditQuestao,
    isImportModalOpen, setIsImportModalOpen,
    handleCopy, handleExplicacaoIA, handleSaveResolucao,
    filtros,
    objetivo, setObjetivo, activeTab, setActiveTab,
    searchTerm, setSearchTerm, showSearchBox, setShowSearchBox,
    selectedMaterias, selectedAssuntos, setSelectedAssuntos,
    selectedBancas, selectedAnos, selectedOrgaos, selectedConcursos,
    selectedCarreiras, selectedEscolaridades, selectedFormacoes, selectedRegioes,
    selectedFavoritas, selectedEnunciados,
    selectedStatus, setSelectedStatus,
    isFilterExpanded, setIsFilterExpanded,
    visibleQuestionsCount, setVisibleQuestionsCount,
    expandedMateriaFolder, setExpandedMateriaFolder,
    cadernoNome, setCadernoNome, pastaDestino, setPastaDestino,
    gerarEmSerie, setGerarEmSerie,
    materiasUnicas, materiasComAssuntos, bancasUnicas, anosUnicos, orgaosUnicos, concursosUnicos,
    filteredQuestions, filteredCount, totalFiltrosAtivos,
    handleToggleMateria, handleToggleAssunto, handleToggleBanca,
    handleToggleAno, handleToggleOrgao, handleToggleConcurso,
    handleToggleCarreira, handleToggleEscolaridade, handleToggleFormacao,
    handleToggleRegiao, handleToggleFavorita, handleToggleEnunciado,
    handleResetFilters, getFilteredQuestions,
    handleGerarCaderno, loadHistoricoDaQuestao,
    ```

    Se algum campo estiver faltando, adicione-o ao return (spread explícito ou campo direto).

    **Não modificar Questoes.tsx** — qualquer erro de tipo significa que o return do orquestrador está incompleto.
  </action>

  <verify>
    <automated>npx tsc -b --noEmit 2>&1; if ($LASTEXITCODE -ne 0) { Write-Host "TYPE ERROR — interface incompleta"; exit 1 } else { Write-Host "Interface OK"; npx vitest run 2>&1; if ($LASTEXITCODE -ne 0) { Write-Host "TEST FAILURE"; exit 1 } else { Write-Host "ALL OK" } }</automated>
  </verify>

  <done>
    - TypeScript compila sem erros
    - ESLint zero erros
    - 38/38 testes passam
    - `Questoes.tsx` não foi modificado
    - Todos os campos da interface original estão presentes no return
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orquestrador → hooks filhos | Wrapper functions cruzam dados entre hooks. Nenhum dado externo não confiável entra. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | E (Elevation of Privilege) | Wrapped actions no orquestrador | accept | Ações são wrappers finos que apenas resolvem `questoesExibidas[currentQuestaoIndex]` e delegam ao hook filho — sem bypass de segurança. |
| T-02-06 | T (Tampering) | `onQuestoesUpdated` callback | mitigate | Callback só aceita `Partial<ResolucaoView>` com spread sobre o objeto existente — apenas sobrescreve campos, não substitui o objeto inteiro. |
</threat_model>

<verification>
- [ ] `npx tsc -b --noEmit` zero erros
- [ ] `npx eslint . --max-warnings=200` zero erros
- [ ] `npx vitest run` — 38/38 testes passam
- [ ] `Questoes.tsx` não foi modificado (git diff deve mostrar só useQuestoes.ts)
- [ ] Todos os campos da interface original estão presentes (verificar com grep)
- [ ] Os 3 hooks especializados são importados e usados
- [ ] Os types re-exportados funcionam (nenhum import quebrado)
</verification>

<success_criteria>
- `src/hooks/useQuestoes.ts` reescrito como orquestrador de ~80-120 linhas
- Compõe `useQuestoesFilter`, `useQuestoesCaderno`, `useQuestoesResolucao`
- Exporta interface idêntica à original
- `Questoes.tsx` não foi modificado
- TypeScript compila limpo, ESLint zero erros, 38/38 testes passam
- Fim da Phase 2 — REFAC-01 completo
</success_criteria>

<output>
After completion, create `.planning/phases/02-extracao-de-hooks/02-03-SUMMARY.md`
</output>
