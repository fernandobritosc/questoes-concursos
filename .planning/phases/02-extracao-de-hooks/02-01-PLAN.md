---
phase: 02-extracao-de-hooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useQuestoesFilter.ts
  - src/hooks/useQuestoesResolucao.ts
autonomous: true
requirements:
  - REFAC-01
user_setup: []

must_haves:
  truths:
    - "useQuestoesFilter expõe todo estado de filtros, funções handleToggle* e dados derivados (materiasUnicas, filteredQuestions, totalFiltrosAtivos)"
    - "useQuestoesFilter expõe buildServerFilters() para uso do orquestrador"
    - "useQuestoesFilter não expõe setters diretamente (exceto setSelectedAssuntos, setActiveTab, setSearchTerm, setShowSearchBox, setSelectedStatus, setIsFilterExpanded)"
    - "useQuestoesResolucao expõe estado de edição, textarea, explicação IA, e funções handleSaveResolucao/handleExplicacaoIA"
    - "useQuestoesResolucao aceita callback onQuestoesUpdated para sincronizar cadernoQuestoes/resolucoes após salvar ou gerar explicação"
    - "TypeScript compila limpo (tsc -b --noEmit) e ESLint zero erros"
  artifacts:
    - path: "src/hooks/useQuestoesFilter.ts"
      provides: "Filter state, toggle actions, derived filter data, buildServerFilters"
      exports: ["useQuestoesFilter", "FilterTab", "ObjetivoFilter", "StatusFilter"]
    - path: "src/hooks/useQuestoesResolucao.ts"
      provides: "Resolução do professor + IA explanation state and actions"
      exports: ["useQuestoesResolucao"]
  key_links:
    - from: "src/hooks/useQuestoesFilter.ts"
      to: "src/types/database.ts"
      via: "import { ResolucaoView }"
      pattern: "import.*ResolucaoView.*database"
    - from: "src/hooks/useQuestoesResolucao.ts"
      to: "src/services/supabase.service.ts"
      via: "import { updateResolucaoProfessor }"
      pattern: "import.*updateResolucaoProfessor.*supabase"
    - from: "src/hooks/useQuestoesResolucao.ts"
      to: "src/services/gemini.service.ts"
      via: "import { gerarResolucaoProfessor }"
      pattern: "import.*gerarResolucaoProfessor.*gemini"

---

<objective>
Criar `useQuestoesFilter` e `useQuestoesResolucao` — dois hooks especializados extraídos do código de `useQuestoes.ts` (~847 linhas).

**Purpose:** Reduzir a complexidade do hook monolítico separando responsabilidades de filtro e resolução do professor.

**Output:** `src/hooks/useQuestoesFilter.ts`, `src/hooks/useQuestoesResolucao.ts`
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-extracao-de-hooks/02-CONTEXT.md
@src/hooks/useQuestoes.ts
@src/types/database.ts
@src/services/supabase.service.ts
@src/services/gemini.service.ts
<!-- @src/services/hermesTracker.ts removed -->
</context>

<interfaces>
Tipo base usado por todos os hooks (de `src/types/database.ts`):

```typescript
export interface ResolucaoView {
  id: number
  questao_id: number
  questao_tec_id: number
  materia: string | null
  assunto: string | null
  banca_texto: string | null
  orgao: string | null
  concurso: string | null
  prova: string | null
  ano: number | null
  caderno_nome: string | null
  enunciado: string | null
  gabarito: string | null
  alternativas: Record<string, string>
  resolucao_professor: string | null
  alternativa: string | null
  acertou: boolean
  tempo_segundos: number
  data_resolucao: string
}
```

Tipos de filtro atualmente definidos em `useQuestoes.ts` (mover para `useQuestoesFilter.ts`):

```typescript
export type FilterTab =
  | 'materia' | 'banca' | 'orgao' | 'ano' | 'carreira'
  | 'escolaridade' | 'formacao' | 'regiao' | 'favoritas'
  | 'enunciados' | 'opcoes'

export type ObjetivoFilter = 'todos' | 'ineditas' | 'discursivas'
export type StatusFilter = 'todos' | 'acertos' | 'erros'
```

Constantes exportadas atualmente de `useQuestoes.ts` (mover para `useQuestoesFilter.ts`):

```typescript
export const CARREIRAS_DISPONIVEIS = ['Policial', 'Fiscal', 'Tribunais', 'Administrativa', 'Saúde', 'Educação', 'Jurídica', 'Militar', 'Legislativa']
export const ESCOLARIDADES_DISPONIVEIS = ['Ensino Médio', 'Ensino Superior', 'Ensino Fundamental']
export const FORMACOES_DISPONIVEIS = ['Qualquer área de formação', 'Direito', 'Administração', 'Contabilidade / Economia', 'Tecnologia da Informação', 'Engenharia', 'Pedagogia', 'Medicina / Enfermagem']
export const REGIOES_DISPONIVEIS = ['Nacional (Federal)', 'São Paulo (SP)', 'Rio de Janeiro (RJ)', 'Minas Gerais (MG)', 'Goiás (GO)', 'Distrito Federal (DF)', 'Região Sul', 'Região Nordeste']
export const FAVORITAS_OPCOES = ['Minhas Favoritas (Estrelas)', 'Questões com Anotações', 'Questões Resolvidas recentemente']
export const ENUNCIADOS_OPCOES = ['Comentadas por Professores', 'Comentadas por IA', 'Sem Comentários', 'Com resolução em Vídeo']
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar useQuestoesFilter.ts com estado de filtros, actions toggles e dados derivados</name>
  <files>
    src/hooks/useQuestoesFilter.ts
  </files>

  <read_first>
    @src/hooks/useQuestoes.ts (linhas 14–51: tipos e constantes; linhas 94–130: estado de filtros; linhas 379–466: dados derivados e actions; linhas 470–513: getFilteredQuestions; linhas 707–729: filteredQuestions e totalFiltrosAtivos)
    @src/types/database.ts (tipo ResolucaoView)
  </read_first>

  <action>
    Criar `src/hooks/useQuestoesFilter.ts` extraindo todo o estado de filtros, constantes, tipos, dados derivados e funções handleToggle* de `useQuestoes.ts`.

    **Estrutura do arquivo:**
    1. **Imports:** `useState`, `useMemo`, `useCallback` do React; `ResolucaoView` de `../types/database`
    2. **Tipos exportados:** `FilterTab`, `ObjetivoFilter`, `StatusFilter`, `ImportStatus` — movidos exatamente como estão de `useQuestoes.ts` (linhas 19–41)
    3. **Constantes exportadas:** `CARREIRAS_DISPONIVEIS`, `ESCOLARIDADES_DISPONIVEIS`, `FORMACOES_DISPONIVEIS`, `REGIOES_DISPONIVEIS`, `FAVORITAS_OPCOES`, `ENUNCIADOS_OPCOES`
    4. **Função `useQuestoesFilter(resolucoes: ResolucaoView[])`:**

    **Estado e setters** (todos `useState`):
    - `objetivo: ObjetivoFilter` padrão `'todos'` + `setObjetivo`
    - `activeTab: FilterTab` padrão `'materia'` + `setActiveTab`
    - `searchTerm: string` padrão `''` + `setSearchTerm`
    - `showSearchBox: boolean` padrão `false` + `setShowSearchBox`
    - `selectedMaterias: string[]` + `setSelectedMaterias`
    - `selectedAssuntos: string[]` + `setSelectedAssuntos`
    - `selectedBancas: string[]` + `setSelectedBancas`
    - `selectedAnos: number[]` + `setSelectedAnos`
    - `selectedOrgaos: string[]` + `setSelectedOrgaos`
    - `selectedConcursos: string[]` + `setSelectedConcursos`
    - `selectedCarreiras: string[]` + `setSelectedCarreiras`
    - `selectedEscolaridades: string[]` + `setSelectedEscolaridades`
    - `selectedFormacoes: string[]` + `setSelectedFormacoes`
    - `selectedRegioes: string[]` + `setSelectedRegioes`
    - `selectedFavoritas: string[]` + `setSelectedFavoritas`
    - `selectedEnunciados: string[]` + `setSelectedEnunciados`
    - `selectedStatus: StatusFilter` padrão `'todos'` + `setSelectedStatus`
    - `isFilterExpanded: boolean` padrão `false` + `setIsFilterExpanded`
    - `visibleQuestionsCount: number` padrão `25` + `setVisibleQuestionsCount`
    - `expandedMateriaFolder: string | null` padrão `null` + `setExpandedMateriaFolder`
    - `cadernoNome: string` padrão `'Caderno de Estudo'` + `setCadernoNome`
    - `pastaDestino: string` padrão `'Analista Legislativo (ALEGO)/2026 - Analista Admi...'` + `setPastaDestino`
    - `gerarEmSerie: boolean` padrão `false` + `setGerarEmSerie`
    - `filtros: Record<string, string> | null` padrão `null` + `setFiltros`

    **Função interna `buildServerFilters`** (movida exatamente de useQuestoes.ts linha 140–153):
    ```typescript
    function buildServerFilters(): Record<string, string[]> {
      const filters: Record<string, string[]> = {}
      if (selectedMaterias.length > 0)   filters.materia = selectedMaterias
      if (selectedBancas.length > 0)    filters.banca_texto = selectedBancas
      if (selectedAnos.length > 0)      filters.ano = selectedAnos.map(String)
      if (selectedOrgaos.length > 0)    filters.orgao = selectedOrgaos
      if (selectedConcursos.length > 0) filters.concurso = selectedConcursos
      return filters
    }
    ```

    **Derivados** (todos `useMemo`):
    - `materiasComAssuntos` — igual useQuestoes.ts linha 381–389 (filtra `resolucoes`)
    - `materiasUnicas` — igual linha 391–394
    - `bancasUnicas` — igual linha 395–397
    - `anosUnicos` — igual linha 399–401
    - `orgaosUnicos` — igual linha 403–405
    - `concursosUnicos` — igual linha 407–409

    **makeToggle helper** (linha 437–438):
    ```typescript
    const makeToggle = <T,>(setter: React.Dispatch<React.SetStateAction<T[]>>) => (value: T) =>
      setter(prev => prev.includes(value) ? prev.filter(v => v !== value) : [...prev, value])
    ```

    **Actions toggles** — movidas exatamente de useQuestoes.ts:
    - `handleToggleMateria` (linhas 414–424)
    - `handleToggleAssunto` (linhas 426–435)
    - `handleToggleBanca` (linha 440 — via makeToggle)
    - `handleToggleAno` (linha 441)
    - `handleToggleOrgao` (linha 442)
    - `handleToggleConcurso` (linha 443)
    - `handleToggleCarreira` (linha 444)
    - `handleToggleEscolaridade` (linha 445)
    - `handleToggleFormacao` (linha 446)
    - `handleToggleRegiao` (linha 447)
    - `handleToggleFavorita` (linha 448)
    - `handleToggleEnunciado` (linha 449)
    - `handleResetFilters` (linhas 451–466)

    **getFilteredQuestions** — movida exatamente de useQuestoes.ts linhas 470–513:
    - Filtra `resolucoes` usando objetivo, selectedMaterias, selectedAssuntos, selectedBancas, selectedAnos, selectedOrgaos, selectedConcursos, selectedCarreiras, selectedStatus
    - A lógica de matching de carreiras (linhas 498–508) deve ser preservada

    **filteredQuestions e filteredCount** — `useMemo` exatamente como linha 709–715

    **totalFiltrosAtivos** — exatamente como linhas 717–729

    **Return** — deve expor TODOS os seguintes campos (a ordem não importa, mas todos devem estar presentes):

    ```typescript
    return {
      // Filter state (read)
      objetivo, setObjetivo,
      activeTab, setActiveTab,
      searchTerm, setSearchTerm,
      showSearchBox, setShowSearchBox,
      selectedMaterias,
      selectedAssuntos, setSelectedAssuntos,
      selectedBancas,
      selectedAnos,
      selectedOrgaos,
      selectedConcursos,
      selectedCarreiras,
      selectedEscolaridades,
      selectedFormacoes,
      selectedRegioes,
      selectedFavoritas,
      selectedEnunciados,
      selectedStatus, setSelectedStatus,
      isFilterExpanded, setIsFilterExpanded,
      visibleQuestionsCount, setVisibleQuestionsCount,
      expandedMateriaFolder, setExpandedMateriaFolder,
      cadernoNome, setCadernoNome,
      pastaDestino, setPastaDestino,
      gerarEmSerie, setGerarEmSerie,
      filtros, setFiltros,

      // Derived / computed
      materiasUnicas,
      materiasComAssuntos,
      bancasUnicas,
      anosUnicos,
      orgaosUnicos,
      concursosUnicos,
      filteredQuestions,
      filteredCount,
      totalFiltrosAtivos,

      // Actions
      handleToggleMateria,
      handleToggleAssunto,
      handleToggleBanca,
      handleToggleAno,
      handleToggleOrgao,
      handleToggleConcurso,
      handleToggleCarreira,
      handleToggleEscolaridade,
      handleToggleFormacao,
      handleToggleRegiao,
      handleToggleFavorita,
      handleToggleEnunciado,
      handleResetFilters,
      getFilteredQuestions,
      buildServerFilters,  // ← para uso do orquestrador
    }
    ```

    **OBSERVAÇÕES IMPORTANTES:**
    - `selectedMaterias`, `selectedBancas`, `selectedAnos`, `selectedOrgaos`, `selectedConcursos`, `selectedCarreiras`, `selectedEscolaridades`, `selectedFormacoes`, `selectedRegioes`, `selectedFavoritas`, `selectedEnunciados` NÃO devem expor setters — apenas o valor. As únicas exceções são: `setSelectedAssuntos`, `setActiveTab`, `setSearchTerm`, `setShowSearchBox`, `setSelectedStatus`, `setIsFilterExpanded` que SÃO expostos para compatibilidade (per D-03).
    - `buildServerFilters` é retornado para uso exclusivo do orquestrador (não faz parte da interface pública documentada)
    - `getFilteredQuestions` é retornado para uso do caderno hook (via orquestrador)
    - `ImportStatus` e seus campos internos não se movem para este hook (ficam no orquestrador)
  </action>

  <verify>
    <automated>npx tsc -b --noEmit 2>&1 | Select-String -NotMatch "info" | Select-String "error"</automated>
  </verify>

  <done>
    - `src/hooks/useQuestoesFilter.ts` existe com todos os campos listados no return
    - Tipos `FilterTab`, `ObjetivoFilter`, `StatusFilter` exportados
    - `npx tsc -b --noEmit` zero erros
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar useQuestoesResolucao.ts com estado de edição e explicação IA</name>
  <files>
    src/hooks/useQuestoesResolucao.ts
  </files>

  <read_first>
    @src/hooks/useQuestoes.ts (linhas 82–86: estado de resolução; linhas 76–77: explicacoes/loadingExplicacao; linhas 536–573: handleExplicacaoIA; linhas 575–600: handleSaveResolucao; linhas 17: type Resolucao alias)
    @src/types/database.ts (tipo ResolucaoView = Resolucao)
    @src/services/supabase.service.ts (updateResolucaoProfessor)
    @src/services/gemini.service.ts (gerarResolucaoProfessor)
    <!-- @src/services/hermesTracker.ts removed --> (trackEvent)
  </read_first>

  <action>
    Criar `src/hooks/useQuestoesResolucao.ts` com estado e ações de resolução do professor e explicação IA.

    **Estrutura do arquivo:**

    1. **Imports:** `useState`, `useCallback` do React; `ResolucaoView` (como `Resolucao`) de `../types/database`; `updateResolucaoProfessor` de `../services/supabase.service`; `gerarResolucaoProfessor` de `../services/gemini.service`

    2. **Interface de opções:**
    ```typescript
    interface UseQuestoesResolucaoOptions {
      /** Callback disparado após uma explicação IA ser gerada, para sincronizar cadernoQuestoes/resolucoes */
      onQuestoesUpdated?: (targetId: number, updates: Partial<ResolucaoView>) => void
    }
    ```

    3. **Função `useQuestoesResolucao(options?: UseQuestoesResolucaoOptions)`:**

    **Estado:**
    - `editingResolucao: boolean` padrão `false` + `setEditingResolucao`
    - `resolucaoText: string` padrão `''` + `setResolucaoText`
    - `resolucaoExpanded: boolean` padrão `true` + `setResolucaoExpanded`
    - `savingResolucao: boolean` padrão `false`
    - `explicacoes: Record<number, string>` padrão `{}`
    - `loadingExplicacao: number | null` padrão `null`

    **Action: `handleSaveResolucao(questaoId: number, text: string): Promise<boolean>`**
    ```typescript
    const handleSaveResolucao = useCallback(async (questaoId: number, text: string): Promise<boolean> => {
      setSavingResolucao(true)
      try {
        await updateResolucaoProfessor(questaoId, text)
        setEditingResolucao(false)
        return true
      } catch (err: unknown) {
        console.error('Erro ao salvar resolução:', err)
        alert('Erro ao salvar a resolução do professor. Verifique sua conexão ou permissões.')
        return false
      } finally {
        setSavingResolucao(false)
      }
    }, [])
    ```

    **Action: `handleExplicacaoIA(questao: ResolucaoView): Promise<void>`**
    - Extraída de useQuestoes.ts linhas 537–573
    - Após gerar a explicação e atualizar `explicacoes`, chama `options.onQuestoesUpdated?.(targetId, { resolucao_professor: texto })` se fornecido
    - Atualiza `resolucaoText` com o texto gerado
    - Remove as chamadas a `setCadernoQuestoes` e `setResolucoes` — estas são responsabilidade do callback `onQuestoesUpdated`
    - Preserva a lógica de `gerarResolucaoProfessor`, `updateResolucaoProfessor`, `trackEvent`, tratamento de erro

    **Return:**
    ```typescript
    return {
      editingResolucao, setEditingResolucao,
      resolucaoText, setResolucaoText,
      resolucaoExpanded, setResolucaoExpanded,
      savingResolucao,
      explicacoes,
      loadingExplicacao,
      handleSaveResolucao,
      handleExplicacaoIA,
    }
    ```

    **OBSERVAÇÕES:**
    - O alias `type Resolucao = ResolucaoView` (linha 15 de useQuestoes.ts) deve ser mantido neste arquivo para compatibilidade interna com o código movido
    - `handleSaveResolucao` agora aceita `questaoId` e `text` como parâmetros (não usa mais `questoesExibidas[currentQuestaoIndex]` ou `resolucaoText` interno) — isso permite que o orquestrador decida qual questão salvar
    - `handleExplicacaoIA` ainda aceita `questao` como parâmetro (já é o padrão atual) e usa `resolucaoText` interno
  </action>

  <verify>
    <automated>npx tsc -b --noEmit 2>&1 | Select-String -NotMatch "info" | Select-String "error"</automated>
  </verify>

  <done>
    - `src/hooks/useQuestoesResolucao.ts` existe com todos os campos listados no return
    - `handleSaveResolucao(questaoId, text)` aceita parâmetros explícitos
    - `handleExplicacaoIA` chama `options.onQuestoesUpdated` para sincronização
    - `npx tsc -b --noEmit` zero erros
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook → componente | Estado e callbacks fluem dos hooks para os componentes. Nenhum dado externo cruza essa fronteira dentro do hook. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T (Tampering) | `handleExplicacaoIA` em `useQuestoesResolucao` | accept | Dado vem do serviço Gemini — sem entrada do usuário neste hook. Risco aceito. |
| T-02-02 | I (Info Disclosure) | `buildServerFilters` exposto publicamente | accept | Função apenas serializa arrays de strings — nenhum dado sensível. |
</threat_model>

<verification>
- [ ] `npx tsc -b --noEmit` zero erros
- [ ] `npx eslint . --max-warnings=200` zero erros (pode ter warnings, mas zero erros)
- [ ] Os 38 testes existentes continuam passando: `npx vitest run 2>&1`
- [ ] `useQuestoesFilter` retorna todos os campos listados (especialmente `buildServerFilters` para o orquestrador)
- [ ] `useQuestoesResolucao` retorna `handleSaveResolucao(questaoId, text)` com a nova assinatura
</verification>

<success_criteria>
- `src/hooks/useQuestoesFilter.ts` existe e compila — exporta useQuestoesFilter, FilterTab, ObjetivoFilter, StatusFilter, constantes
- `src/hooks/useQuestoesResolucao.ts` existe e compila — exporta useQuestoesResolucao
- TypeScript compila limpo, ESLint zero erros, 38/38 testes passam
- O código existente em `useQuestoes.ts` ainda não foi modificado (a refatoração do orquestrador é o Plan 3)
</success_criteria>

<output>
After completion, create `.planning/phases/02-extracao-de-hooks/02-01-SUMMARY.md`
</output>
