---
phase: 02-extracao-de-hooks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useQuestoesCaderno.ts
autonomous: true
requirements:
  - REFAC-01
user_setup: []

must_haves:
  truths:
    - "useQuestoesCaderno expõe estado do caderno (cadernoQuestoes, isCadernoActive, currentQuestaoIndex, alternativaSelecionada, revelado, copiedId, tempoSegundos, salvandoResposta, historicoQuestaoAtiva, loadingHistoricoAtivo)"
    - "useQuestoesCaderno expõe setters para navegação (setCurrentQuestaoIndex, setAlternativaSelecionada, setRevelado, setIsCadernoActive, setCadernoQuestoes)"
    - "useQuestoesCaderno expõe actions (handleConfirmarResposta, handleEditQuestao, handleCopy, loadHistoricoDaQuestao)"
    - "handleGerarCaderno é substituído por acceptFilteredQuestions(filteredQuestions, totalFiltrosAtivos) e setCadernoFromFilteredQuestions (o orquestrador faz o wrapping)"
    - "useQuestoesCaderno aceita parâmetros: getFilteredQuestions, resolucoes, setResolucoes"
    - "Os effects de timer e de loadHistorico NÃO estão neste hook (ficam no orquestrador pois dependem de questoesExibidas)"
    - "TypeScript compila limpo e ESLint zero erros"
  artifacts:
    - path: "src/hooks/useQuestoesCaderno.ts"
      provides: "Caderno de estudo, navegação, resposta, edição, cópia, histórico de questões"
      exports: ["useQuestoesCaderno"]
  key_links:
    - from: "src/hooks/useQuestoesCaderno.ts"
      to: "src/types/database.ts"
      via: "import { ResolucaoView }"
      pattern: "import.*ResolucaoView.*database"
    - from: "src/hooks/useQuestoesCaderno.ts"
      to: "src/services/supabase.service.ts"
      via: "import { insertHistoricoResolucao, updateQuestao, fetchHistoricoByQuestao }"
      pattern: "import.*insertHistoricoResolucao.*supabase"

---

<objective>
Criar `useQuestoesCaderno` — hook especializado extraído de `useQuestoes.ts` (~847 linhas) que gerencia o caderno de estudo, navegação entre questões, confirmação de resposta, edição e histórico.

**Purpose:** Separar a responsabilidade do caderno (navegação, respostas, edição) em um hook independente que aceita callbacks para coordenar com o orquestrador.

**Output:** `src/hooks/useQuestoesCaderno.ts`
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-extracao-de-hooks/02-CONTEXT.md
@src/hooks/useQuestoes.ts
@src/types/database.ts
@src/services/supabase.service.ts
<!-- @src/services/hermesTracker.ts removed -->
</context>

<interfaces>
Tipo base (de `src/types/database.ts`):

```typescript
export interface ResolucaoView {
  id: number
  questao_id: number
  questao_tec_id: number
  materia: string | null
  assunto: string | null
  banca_texto: string | null
  orgao: string | null
  concurso: string | null
  prova: string | null
  ano: number | null
  caderno_nome: string | null
  enunciado: string | null
  gabarito: string | null
  alternativas: Record<string, string>
  resolucao_professor: string | null
  alternativa: string | null
  acertou: boolean
  tempo_segundos: number
  data_resolucao: string
}
```

Interface de parâmetros que o caderno hook aceita:
```typescript
interface UseQuestoesCadernoParams {
  /** Função do filter hook que retorna questões filtradas (usada em handleGerarCaderno) */
  getFilteredQuestions: () => ResolucaoView[]
  /** Lista total de resoluções (do orquestrador) — usada para sync em handleConfirmarResposta e handleEditQuestao */
  resolucoes: ResolucaoView[]
  /** Setter de resolucoes (do orquestrador) */
  setResolucoes: React.Dispatch<React.SetStateAction<ResolucaoView[]>>
}
```

**Nota sobre o que NÃO está neste hook (permanece no orquestrador):**
- `questoesExibidas` (derivado de cadernoQuestoes + filtros)
- Timer effects (dependem de questoesExibidas e currentQuestaoIndex)
- Timer reset effect
- History loading effect (depende de questoesExibidas)
- Navigation sync (resolucaoText sync — depende de questoesExibidas e estado da resolucao hook)
- `handleGerarCaderno` wrapper (chama getFilteredQuestions do filter + setters do caderno)
- `handleConfirmarResposta` wrapper (resolve questao atual de questoesExibidas)
- `handleEditQuestao` wrapper (resolve questao atual de questoesExibidas)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar useQuestoesCaderno.ts com estado, navegação, resposta, edição e histórico</name>
  <files>
    src/hooks/useQuestoesCaderno.ts
  </files>

  <read_first>
    @src/hooks/useQuestoes.ts (linhas 70–80: caderno state; linhas 88–92: timer/history state; linhas 350–360: loadHistoricoDaQuestao; linhas 515–533: handleGerarCaderno, handleCopy; linhas 602–660: handleConfirmarResposta; linhas 662–705: handleEditQuestao)
    @src/types/database.ts (ResolucaoView, HistoricoResolucao)
    @src/services/supabase.service.ts (insertHistoricoResolucao, updateQuestao, fetchHistoricoByQuestao)
    <!-- @src/services/hermesTracker.ts removed --> (trackEvent)
  </read_first>

  <action>
    Criar `src/hooks/useQuestoesCaderno.ts` com estado e ações do caderno de estudo.

    **Estrutura do arquivo:**

    1. **Imports:** `useState`, `useCallback` do React; `ResolucaoView`, `HistoricoResolucao` de `../types/database`; `insertHistoricoResolucao`, `updateQuestao`, `fetchHistoricoByQuestao` de `../services/supabase.service`

    2. **Interface de parâmetros:**
    ```typescript
    interface UseQuestoesCadernoParams {
      getFilteredQuestions: () => ResolucaoView[]
      resolucoes: ResolucaoView[]
      setResolucoes: React.Dispatch<React.SetStateAction<ResolucaoView[]>>
    }
    ```

    3. **Função `useQuestoesCaderno(params: UseQuestoesCadernoParams)`:**

    **Estado do caderno:**
    - `cadernoQuestoes: ResolucaoView[]` padrão `[]` + `setCadernoQuestoes`
    - `isCadernoActive: boolean` padrão `false` + `setIsCadernoActive`
    - `currentQuestaoIndex: number` padrão `0` + `setCurrentQuestaoIndex`
    - `alternativaSelecionada: string | null` padrão `null` + `setAlternativaSelecionada`
    - `revelado: boolean` padrão `false` + `setRevelado`
    - `copiedId: number | null` padrão `null`

    **Estado de timer e histórico (só os valores — os effects ficam no orquestrador):**
    - `tempoSegundos: number` padrão `0` + `setTempoSegundos`
    - `salvandoResposta: boolean` padrão `false`
    - `historicoQuestaoAtiva: HistoricoResolucao[]` padrão `[]` + `setHistoricoQuestaoAtiva`
    - `loadingHistoricoAtivo: boolean` padrão `false`

    **Action: `handleGerarCaderno()` — aceita parâmetros externos**
    ```typescript
    const handleGerarCaderno = useCallback(() => {
      const questoesFiltradas = params.getFilteredQuestions()
      if (questoesFiltradas.length === 0) return
      setCadernoQuestoes(questoesFiltradas)
      setCurrentQuestaoIndex(0)
      setAlternativaSelecionada(null)
      setRevelado(false)
      setIsCadernoActive(true)
      return questoesFiltradas.length  // retorna quantidade para o orquestrador fazer trackEvent
    }, [params.getFilteredQuestions])  // NOTA: dependência mínima — getFilteredQuestions é estável
    ```
    **IMPORTANTE:** `handleGerarCaderno` NÃO chama `trackEvent` nem `totalFiltrosAtivos` — o orquestrador fará o wrapping com essas informações.

    **Action: `handleConfirmarResposta(questao: ResolucaoView): Promise<void>`**
    - Extraída de useQuestoes.ts linhas 602–660
    - Aceita `questao` como parâmetro em vez de resolver de `questoesExibidas[currentQuestaoIndex]`
    - Atualiza `cadernoQuestoes`, `params.resolucoes` (via `params.setResolucoes`), e estado interno (`revelado`, `salvandoResposta`)
    - Chama `loadHistoricoDaQuestao(targetId)` internamente (assim como no código original)
    - Preserva lógica de `insertHistoricoResolucao`, `trackEvent`
    - Usa `tempoSegundos` do estado interno

    **Action: `handleEditQuestao(questao: ResolucaoView, updatedFields: Partial<ResolucaoView>): Promise<boolean>`**
    - Extraída de useQuestoes.ts linhas 662–705
    - Aceita `questao` como parâmetro em vez de resolver de `questoesExibidas[currentQuestaoIndex]`
    - Atualiza `cadernoQuestoes` e `params.resolucoes` (via `params.setResolucoes`)
    - Preserva a lógica de `updateQuestao`, `trackEvent`, tratamento de erro

    **Action: `handleCopy(id: number): void`**
    - Extraída de useQuestoes.ts linhas 529–533
    - Copia para clipboard e gerencia `copiedId` com setTimeout de 2000ms

    **Action: `loadHistoricoDaQuestao(questaoId: number): Promise<void>`**
    - Extraída de useQuestoes.ts linhas 350–360
    - Carrega histórico via `fetchHistoricoByQuestao` e atualiza `historicoQuestaoAtiva`

    **Return:**
    ```typescript
    return {
      // Estado do caderno
      cadernoQuestoes, setCadernoQuestoes,
      isCadernoActive, setIsCadernoActive,
      currentQuestaoIndex, setCurrentQuestaoIndex,
      alternativaSelecionada, setAlternativaSelecionada,
      revelado, setRevelado,
      copiedId,

      // Timer e histórico (valores puros — effects no orquestrador)
      tempoSegundos, setTempoSegundos,
      salvandoResposta,
      historicoQuestaoAtiva, setHistoricoQuestaoAtiva,
      loadingHistoricoAtivo,

      // Actions
      handleGerarCaderno,
      handleConfirmarResposta,
      handleEditQuestao,
      handleCopy,
      loadHistoricoDaQuestao,
    }
    ```

    **OBSERVAÇÕES IMPORTANTES:**
    - `handleGerarCaderno` retorna o número de questões filtradas (ou 0/undefined se vazio) para que o orquestrador possa chamar `trackEvent` com `totalFiltrosAtivos` do filter hook
    - `handleConfirmarResposta(questao)` aceita a questão como parâmetro — o orquestrador resolve `questoesExibidas[currentQuestaoIndex]` e passa
    - `handleEditQuestao(questao, updatedFields)` aceita a questão como parâmetro — mesmo padrão
    - Os seguintes effects NÃO estão neste hook (ficam no orquestrador, Plan 3):
      - Timer increment (`setInterval` — depende de `revelado` e `questoesExibidas`)
      - Timer reset (depende de `currentQuestaoIndex` alterado)
      - Load histórico on navigation (depende de `currentQuestaoIndex` e `questoesExibidas`)
      - Sync resolucaoText on navigation (depende de `currentQuestaoIndex`, `questoesExibidas`, e estado do resolucao hook)
  </action>

  <verify>
    <automated>npx tsc -b --noEmit 2>&1 | Select-String -NotMatch "info" | Select-String "error"</automated>
  </verify>

  <done>
    - `src/hooks/useQuestoesCaderno.ts` existe com todos os campos listados no return
    - `handleConfirmarResposta(questao)` aceita `ResolucaoView` como parâmetro
    - `handleEditQuestao(questao, updatedFields)` aceita a questão + campos como parâmetros
    - `handleGerarCaderno()` retorna `number | undefined` (quantidade de questões filtradas)
    - `npx tsc -b --noEmit` zero erros
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook → banco | Dados de resposta do usuário (alternativa selecionada) fluem para o Supabase via `insertHistoricoResolucao` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | T (Tampering) | `handleConfirmarResposta` em `useQuestoesCaderno` | accept | Dado de `alternativaSelecionada` vem do estado interno (não de input externo). Risco mínimo. |
| T-02-04 | T (Tampering) | `handleEditQuestao` — escrita em `updateQuestao` | mitigate | Payload é construído com spread controlado (apenas campos explícitos em `updatedFields` são incluídos). Nenhum campo bruto de entrada do usuário é passado. |
</threat_model>

<verification>
- [ ] `npx tsc -b --noEmit` zero erros
- [ ] `npx eslint . --max-warnings=200` zero erros
- [ ] `npx vitest run 2>&1` — 38/38 testes passam
- [ ] O código existente em `useQuestoes.ts` ainda não foi modificado (a refatoração do orquestrador é o Plan 3)
</verification>

<success_criteria>
- `src/hooks/useQuestoesCaderno.ts` existe e compila — exporta `useQuestoesCaderno`
- Aceita `UseQuestoesCadernoParams` com `getFilteredQuestions`, `resolucoes`, `setResolucoes`
- Actions aceitam parâmetros explícitos (questao, questaoId, etc.) em vez de depender do escopo fechado
- TypeScript compila limpo, ESLint zero erros, 38/38 testes passam
</success_criteria>

<output>
After completion, create `.planning/phases/02-extracao-de-hooks/02-02-SUMMARY.md`
</output>
